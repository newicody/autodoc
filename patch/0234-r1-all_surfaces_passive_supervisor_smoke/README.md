# 0234-r1-all_surfaces_passive_supervisor_smoke

Adds an all-surfaces passive supervisor smoke CLI and tests.

Apply with:

```bash
python apply_patch_queue.py --patch 0234-r1-all_surfaces_passive_supervisor_smoke --dry-run --allow-dirty
python apply_patch_queue.py --patch 0234-r1-all_surfaces_passive_supervisor_smoke --commit --push --allow-dirty
```
