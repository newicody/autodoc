# 0236-r1-passive_supervisor_visual_read_model

Adds a read-only visual read-model derived from passive supervisor snapshots.

Apply:

```bash
python apply_patch_queue.py --patch 0236-r1-passive_supervisor_visual_read_model --dry-run --allow-dirty
python apply_patch_queue.py --patch 0236-r1-passive_supervisor_visual_read_model --commit --push --allow-dirty
```
