# 0284-r1-r4 — Projects bundle Copilot visibility and views

This patch applies after the completed 0284-r1-r3 configuration split.

It changes only the source bundle copied into `newicody/projects`, plus its
Autodoc phase reports and tests. It does not activate a Project mode in
Autodoc and does not change the active GitHub Actions workflow permissions.

Suggested commit:

```text
add-projects-bundle-copilot-visibility-and-views
```
