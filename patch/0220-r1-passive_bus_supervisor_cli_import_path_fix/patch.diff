diff --git a/tools/run_passive_bus_supervisor_cellular_snapshot_0220.py b/tools/run_passive_bus_supervisor_cellular_snapshot_0220.py
index 1111111..2222222 100644
--- a/tools/run_passive_bus_supervisor_cellular_snapshot_0220.py
+++ b/tools/run_passive_bus_supervisor_cellular_snapshot_0220.py
@@ -5,10 +5,15 @@ from __future__ import annotations
 
 import argparse
 import json
+import sys
 from datetime import datetime, timezone
 from pathlib import Path
 from typing import Any
 
+ROOT = Path(__file__).resolve().parents[1]
+if str(ROOT) not in sys.path:
+    sys.path.insert(0, str(ROOT))
+
 from src.context.passive_bus_supervisor_cellular_snapshot import (
     build_cellular_snapshot,
     event_from_mapping,
diff --git a/doc/CHANGELOG_0220_PASSIVE_BUS_SUPERVISOR_CELLULAR_SNAPSHOT.md b/doc/CHANGELOG_0220_PASSIVE_BUS_SUPERVISOR_CELLULAR_SNAPSHOT.md
index 1111111..2222222 100644
--- a/doc/CHANGELOG_0220_PASSIVE_BUS_SUPERVISOR_CELLULAR_SNAPSHOT.md
+++ b/doc/CHANGELOG_0220_PASSIVE_BUS_SUPERVISOR_CELLULAR_SNAPSHOT.md
@@ -33,3 +33,9 @@ view over the emitted snapshot.
 This patch was prepared in a sandbox where the Autodoc checkout was not mounted
 and GitHub access was blocked for local clone. Validation must be run in the real
 repository through the patch queue.
+
+## Fixed in 0220-r1
+
+- Fixed direct CLI execution by inserting the repository root into `sys.path`
+  before importing `src.context.passive_bus_supervisor_cellular_snapshot`.
+- Kept the passive supervisor authority boundary unchanged.
diff --git a/PHASE0220_TEST_REPORT.md b/PHASE0220_TEST_REPORT.md
index 1111111..2222222 100644
--- a/PHASE0220_TEST_REPORT.md
+++ b/PHASE0220_TEST_REPORT.md
@@ -40,3 +40,11 @@ and local GitHub clone is blocked. The patch is
 ## Non-Stdlib Dependencies
 
 None.
+
+## 0220-r1 Follow-Up
+
+The first real repository run found one subprocess-only failure in
+`tests/tools/test_run_passive_bus_supervisor_cellular_snapshot_0220.py`: direct
+execution of the CLI could not reliably import `src.context...` because Python
+starts with `tools/` on `sys.path`. Patch `0220-r1` adds the repository root to
+`sys.path` before project imports.
