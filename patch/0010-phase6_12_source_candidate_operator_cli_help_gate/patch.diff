diff --git a/MANIFEST_PHASE6_12_CHANGED_FILES.md b/MANIFEST_PHASE6_12_CHANGED_FILES.md
new file mode 100644
index 0000000..e1fffa1
--- /dev/null
+++ b/MANIFEST_PHASE6_12_CHANGED_FILES.md
@@ -0,0 +1,19 @@
+# Phase 6.12 changed files — SourceCandidate Operator CLI Help Gate
+
+## Added
+
+- `tests/context/test_source_candidate_operator_cli_help.py` — smoke tests for the unified operator CLI help surface.
+- `tests/rules/test_source_candidate_operator_cli_help_rule.py` — rule test keeping the unified CLI as an adapter-only surface.
+- `doc/docs/architecture/context/53_source_candidate_operator_cli_help_gate.dot` — architecture note for the help/preflight gate.
+- `doc/CHANGELOG_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md` — changelog.
+- `doc/PHASE6_12_CODE_RULE_ALIGNMENT.md` — code-rule alignment report.
+- `doc/releases/README_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md` — phase release README.
+- `PHASE6_12_TEST_REPORT.md` — test report.
+
+## Modified
+
+None.
+
+## Generated artifacts
+
+No SVG files are versioned. Generate them locally with `make -C doc` and remove them with `make -C doc clean` before push.
diff --git a/PHASE6_12_TEST_REPORT.md b/PHASE6_12_TEST_REPORT.md
new file mode 100644
index 0000000..62c2d32
--- /dev/null
+++ b/PHASE6_12_TEST_REPORT.md
@@ -0,0 +1,33 @@
+# Phase 6.12 test report — SourceCandidate Operator CLI Help Gate
+
+## Scope
+
+Phase 6.12 adds a small regression gate for the unified SourceCandidate operator CLI introduced in Phase 6.11.
+
+It does not add a new backend, Scheduler event, store mutation, network integration, Qdrant integration, LLM, or OpenVINO runtime path.
+
+## Expected gate
+
+```bash
+PYTHONPATH=src python -m compileall -q src tests
+PYTHONPATH=src pytest -q tests/context/test_source_candidate_operator_cli_help.py
+PYTHONPATH=src pytest -q tests/rules
+PYTHONPATH=src pytest -q
+```
+
+## Code rule block
+
+```text
+code_rule_review: done
+code_rule_update_required: false
+code_rule_reason: Phase 6.12 adds regression tests and documentation for the unified operator CLI help surface only.
+live_path_status: n/a
+live_path_uses_real_backend: n/a
+external_dependencies_added: false
+scheduler_modified: false
+network_added: false
+github_api_added: false
+qdrant_added: false
+llm_or_openvino_added: false
+search_commands_bounded: n/a
+```
diff --git a/doc/CHANGELOG_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md b/doc/CHANGELOG_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md
new file mode 100644
index 0000000..1257fd0
--- /dev/null
+++ b/doc/CHANGELOG_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md
@@ -0,0 +1,17 @@
+# Changelog — Phase 6.12 SourceCandidate Operator CLI Help Gate
+
+## Added
+
+- Added smoke tests for `python -m context.source_candidate_operator_cli --help`.
+- Added subcommand help smoke coverage for the unified operator command surface.
+- Added a rule test that keeps `source_candidate_operator_cli.py` adapter-only.
+- Added DOT and release documentation for the help/preflight gate.
+
+## Not added
+
+- No new SourceCandidate business capability.
+- No new Scheduler event.
+- No network call.
+- No GitHub API.
+- No Qdrant, LLM, or OpenVINO path.
+- No generated SVG artifact.
diff --git a/doc/PHASE6_12_CODE_RULE_ALIGNMENT.md b/doc/PHASE6_12_CODE_RULE_ALIGNMENT.md
new file mode 100644
index 0000000..1737be5
--- /dev/null
+++ b/doc/PHASE6_12_CODE_RULE_ALIGNMENT.md
@@ -0,0 +1,29 @@
+# Phase 6.12 code-rule alignment — SourceCandidate Operator CLI Help Gate
+
+Phase 6.12 is a regression-gate phase. It stabilizes the operator command surface added in Phase 6.11.
+
+## Alignment
+
+- The unified CLI remains an adapter surface.
+- Business logic remains in pure context modules and live Scheduler paths added in earlier phases.
+- No Scheduler change is introduced.
+- No external dependency is introduced.
+- No network operation is introduced.
+- No generated SVG is versioned.
+
+## Required report block
+
+```text
+code_rule_review: done
+code_rule_update_required: false
+code_rule_reason: Existing Phase 6-r3 patch queue and CLI-adapter rules already cover this phase.
+live_path_status: n/a
+live_path_uses_real_backend: n/a
+external_dependencies_added: false
+scheduler_modified: false
+network_added: false
+github_api_added: false
+qdrant_added: false
+llm_or_openvino_added: false
+search_commands_bounded: n/a
+```
diff --git a/doc/releases/README_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md b/doc/releases/README_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md
new file mode 100644
index 0000000..b4b9629
--- /dev/null
+++ b/doc/releases/README_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md
@@ -0,0 +1,27 @@
+# Phase 6.12 — SourceCandidate Operator CLI Help Gate
+
+This phase adds a small stability gate for the unified SourceCandidate operator CLI.
+
+The goal is to make sure the consolidated command surface remains discoverable and adapter-only.
+
+## Command surface guarded
+
+```bash
+PYTHONPATH=src python -m context.source_candidate_operator_cli --help
+```
+
+Expected commands include:
+
+```text
+intake
+review
+decide
+review-audit
+report
+report-file
+bundle
+```
+
+## Scope
+
+This is not a new business capability. It is a regression and documentation phase.
diff --git a/doc/docs/architecture/context/53_source_candidate_operator_cli_help_gate.dot b/doc/docs/architecture/context/53_source_candidate_operator_cli_help_gate.dot
new file mode 100644
index 0000000..34e006e
--- /dev/null
+++ b/doc/docs/architecture/context/53_source_candidate_operator_cli_help_gate.dot
@@ -0,0 +1,19 @@
+digraph source_candidate_operator_cli_help_gate {
+  graph [rankdir=LR, label="Phase 6.12 — SourceCandidate Operator CLI Help Gate", labelloc=t];
+  node [shape=box];
+
+  operator [label="operator"];
+  unified_cli [label="source_candidate_operator_cli
+(adapter only)"];
+  command_help [label="--help / subcommand --help"];
+  existing_cli [label="existing SourceCandidate CLI modules"];
+  pure_modules [label="pure context modules / live paths"];
+  tests [label="tests + rules"];
+
+  operator -> unified_cli;
+  unified_cli -> command_help;
+  unified_cli -> existing_cli [label="delegates"];
+  existing_cli -> pure_modules;
+  tests -> unified_cli [label="guard discoverability"];
+  tests -> existing_cli [label="no backend ownership"];
+}
diff --git a/tests/context/test_source_candidate_operator_cli_help.py b/tests/context/test_source_candidate_operator_cli_help.py
new file mode 100644
index 0000000..bf6d20b
--- /dev/null
+++ b/tests/context/test_source_candidate_operator_cli_help.py
@@ -0,0 +1,48 @@
+from __future__ import annotations
+
+import os
+import subprocess
+import sys
+
+
+_EXPECTED_COMMANDS = (
+    "intake",
+    "review",
+    "decide",
+    "review-audit",
+    "report",
+    "report-file",
+    "bundle",
+)
+
+
+def _env() -> dict[str, str]:
+    env = dict(os.environ)
+    env["PYTHONPATH"] = "src" + (os.pathsep + env["PYTHONPATH"] if env.get("PYTHONPATH") else "")
+    return env
+
+
+def test_operator_cli_root_help_lists_expected_commands() -> None:
+    result = subprocess.run(
+        [sys.executable, "-m", "context.source_candidate_operator_cli", "--help"],
+        check=True,
+        env=_env(),
+        text=True,
+        stdout=subprocess.PIPE,
+    )
+
+    assert "source_candidate_operator_cli" in result.stdout or "SourceCandidate" in result.stdout
+    for command in _EXPECTED_COMMANDS:
+        assert command in result.stdout
+
+
+def test_operator_cli_subcommand_help_is_available() -> None:
+    for command in _EXPECTED_COMMANDS:
+        result = subprocess.run(
+            [sys.executable, "-m", "context.source_candidate_operator_cli", command, "--help"],
+            check=True,
+            env=_env(),
+            text=True,
+            stdout=subprocess.PIPE,
+        )
+        assert result.stdout.strip()
diff --git a/tests/rules/test_source_candidate_operator_cli_help_rule.py b/tests/rules/test_source_candidate_operator_cli_help_rule.py
new file mode 100644
index 0000000..9f31cf9
--- /dev/null
+++ b/tests/rules/test_source_candidate_operator_cli_help_rule.py
@@ -0,0 +1,32 @@
+from __future__ import annotations
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+
+
+def test_operator_cli_remains_adapter_only() -> None:
+    path = ROOT / "src" / "context" / "source_candidate_operator_cli.py"
+    text = path.read_text(encoding="utf-8").lower()
+
+    forbidden = (
+        "sourcecandidatestorepolicy(",
+        "upsert_source_candidate",
+        "load_source_candidate_store",
+        "eventtype.source_candidate",
+        "scheduler(",
+        "priorityqueue(",
+        "requests",
+        "qdrant",
+        "openvino",
+    )
+    for token in forbidden:
+        assert token not in text
+
+
+def test_operator_cli_help_gate_documents_expected_commands() -> None:
+    readme = ROOT / "doc" / "releases" / "README_PHASE6_12_SOURCE_CANDIDATE_OPERATOR_CLI_HELP.md"
+    text = readme.read_text(encoding="utf-8")
+    for command in ("intake", "review", "decide", "review-audit", "report", "report-file", "bundle"):
+        assert command in text
