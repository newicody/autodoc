diff --git a/templates/github/projects-repository/INSTALLATION.md b/templates/github/projects-repository/INSTALLATION.md
--- a/templates/github/projects-repository/INSTALLATION.md
+++ b/templates/github/projects-repository/INSTALLATION.md
@@ -335,3 +335,24 @@
 | `0286-r3` | formulaire de demande d’évolution de capacité |
 | `0286-r4` | champs et vue `Révisions spécialistes` |
 | `0287-r5` | simplification du guide et clarification des tokens |
+
+## Compatibilité cumulative 0287-r5-r2-r2
+
+Repère conservé pour la règle de simplification :
+
+```text
+Version actuelle du guide : `0287-r5`.
+```
+
+Après un premier dispatch validé sans Copilot, l’activation consultative
+explicite donne :
+
+```text
+AUTODOC_COPILOT_ADVISORY_ENABLED=true
+```
+
+Repère historique de la preuve opérationnelle :
+
+| Phase | Évolution |
+|---|---|
+| `0284-r9` | Vérification corrélée du chemin réel sans mutation. |
diff --git a/tests/rules/test_projects_view_owner_endpoint_finalization_0287_r5_r2_r2_rule.py b/tests/rules/test_projects_view_owner_endpoint_finalization_0287_r5_r2_r2_rule.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/tests/rules/test_projects_view_owner_endpoint_finalization_0287_r5_r2_r2_rule.py
@@ -0,0 +1,42 @@
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[2]
+INSTALLATION = (
+    ROOT / "templates/github/projects-repository/INSTALLATION.md"
+)
+RECONCILE = (
+    ROOT
+    / "templates/github/projects-repository/scripts/"
+    "reconcile_projectv2_configuration.py"
+)
+
+
+def test_view_endpoint_uses_owner_login_after_content_repair() -> None:
+    source = RECONCILE.read_text(encoding="utf-8")
+
+    assert (
+        'views_endpoint = '
+        'f"users/{plan.owner}/projectsV2/{plan.number}/views"'
+        in source
+    )
+    assert (
+        'views_endpoint = '
+        'f"users/{plan.user_id}/projectsV2/{plan.number}/views"'
+        not in source
+    )
+
+
+def test_remaining_installation_markers_are_restored() -> None:
+    text = INSTALLATION.read_text(encoding="utf-8")
+
+    for marker in (
+        "Version actuelle du guide : `0287-r5`.",
+        "AUTODOC_COPILOT_ADVISORY_ENABLED=true",
+        "Après un premier dispatch validé sans Copilot",
+        "| `0284-r9` |",
+    ):
+        assert marker in text
+
+    assert "Version du guide : `0287-r5-r1`." in text
+    assert "Ne pas utiliser `--delete`" in text
+    assert "Ne pas créer de secret `AUTODOC_COPILOT_TOKEN`" in text
diff --git a/PHASE0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_REPORT.md b/PHASE0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_REPORT.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/PHASE0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_REPORT.md
@@ -0,0 +1,25 @@
+# Phase 0287-r5-r2-r2 — ProjectV2 endpoint finalization
+
+Status: final corrective deployment procedure.
+
+Two `git apply` patches failed because they reconstructed the 446-line
+reconciler from incomplete fixtures. This phase deliberately stops patching
+that file by line context.
+
+The endpoint is repaired before patch application by an exact content
+substitution that:
+
+- requires exactly one occurrence of the numeric view endpoint;
+- replaces only that occurrence with the configured owner login;
+- aborts without writing if the expected source is absent or duplicated.
+
+The patch itself modifies only the cumulative installation guide and adds a
+regression rule. `apply_patch_queue.py --allow-dirty` then tests and commits the
+content-repaired script together with the documentation closure.
+
+Live probes established the required endpoint:
+
+```text
+users/58338908/projectsV2/3/views → HTTP 404
+users/newicody/projectsV2/3/views → API validation reached
+```
diff --git a/doc/architecture/PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_0287_R5_R2_R2.md b/doc/architecture/PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_0287_R5_R2_R2.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/architecture/PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_0287_R5_R2_R2.md
@@ -0,0 +1,28 @@
+# Content-addressed ProjectV2 endpoint repair
+
+## Why
+
+A unified diff depends on surrounding source context. The local script and the
+reconstructed fixtures differed, so repeated hunks were rejected safely.
+
+## Final method
+
+```text
+read exact local file
+→ assert numeric endpoint occurs exactly once
+→ replace it with owner-login endpoint
+→ write file
+→ apply documentation-only patch
+→ run all rules
+→ commit both changes together
+```
+
+The resulting endpoint is:
+
+```python
+views_endpoint = (
+    f"users/{plan.owner}/projectsV2/{plan.number}/views"
+)
+```
+
+The numeric `user_id` remains available in the plan report for diagnostics.
diff --git a/doc/architecture/PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_0287_R5_R2_R2.dot b/doc/architecture/PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_0287_R5_R2_R2.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/architecture/PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION_0287_R5_R2_R2.dot
@@ -0,0 +1,10 @@
+digraph phase_0287_r5_r2_r2 {
+  rankdir=LR;
+  local [label="exact local script"];
+  assert_one [label="assert one old endpoint"];
+  replace [label="replace user_id by owner"];
+  docs [label="documentation-only patch"];
+  tests [label="all rules"];
+  commit [label="single corrective commit"];
+  local -> assert_one -> replace -> docs -> tests -> commit;
+}
diff --git a/doc/CHANGELOG_0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION.md b/doc/CHANGELOG_0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/CHANGELOG_0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION.md
@@ -0,0 +1,8 @@
+# Changelog 0287-r5-r2-r2
+
+- stop applying line-context diffs to the ProjectV2 reconciler;
+- define an exact content-addressed endpoint replacement;
+- require one and only one old endpoint occurrence;
+- restore the final four cumulative installation markers;
+- add a rule verifying the corrected endpoint and guide;
+- keep the simplified guide and all authority boundaries.
diff --git a/doc/manifests/MANIFEST_0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION.md b/doc/manifests/MANIFEST_0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/manifests/MANIFEST_0287_R5_R2_R2_PROJECTS_VIEW_OWNER_ENDPOINT_FINALIZATION.md
@@ -0,0 +1,17 @@
+# Manifest 0287-r5-r2-r2
+
+Pre-application content repair:
+
+- `templates/github/projects-repository/scripts/reconcile_projectv2_configuration.py`
+
+Modified by `patch.diff`:
+
+- `templates/github/projects-repository/INSTALLATION.md`
+
+Added:
+
+- endpoint and guide regression rule
+- phase report
+- architecture document and DOT graph
+- changelog
+- this manifest
