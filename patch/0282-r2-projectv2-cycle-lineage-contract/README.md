# 0282-r2-projectv2-cycle-lineage-contract

Adds the pure immutable ProjectV2 cycle-lineage contract justified by the r1
reuse audit.

```text
Repo autodoc: OUI
Repo projects: NON
new_runtime_module_added: true
new_cli_added: false
new_adapter_added: false
projects_repository_change_required: false
```

Apply:

```bash
python apply_patch_queue.py \
  --patch 0282-r2-projectv2-cycle-lineage-contract \
  --dry-run \
  --allow-dirty
```
