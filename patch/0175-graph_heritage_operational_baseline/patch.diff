diff --git a/PHASE0175_TEST_REPORT.md b/PHASE0175_TEST_REPORT.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/PHASE0175_TEST_REPORT.md
@@ -0,0 +1,19 @@
+# Phase 0175 Test Report — Graph heritage and operational baseline
+
+Status: prepared.
+
+Scope:
+- Documentation/rule/graph contract only.
+- No merge into `00_global.dot`.
+- Keeps old graphs as heritage/orientation/inspiration.
+- Locks 0174 rebuilt graph draft as the immediate operational baseline.
+- No runtime code.
+
+Validation:
+
+```bash
+python -m compileall -q src tests tools
+python -m pytest -q tests/rules/test_graph_heritage_operational_baseline_0175_rule.py
+python -m pytest -q tests/rules
+python -m pytest -q
+```
diff --git a/doc/architecture/GRAPH_HERITAGE_OPERATIONAL_BASELINE_0175.md b/doc/architecture/GRAPH_HERITAGE_OPERATIONAL_BASELINE_0175.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/architecture/GRAPH_HERITAGE_OPERATIONAL_BASELINE_0175.md
@@ -0,0 +1,99 @@
+# 0175 — Graph heritage and operational baseline
+
+## Decision
+
+0175 explicitly avoids merging or replacing the old global graph.
+
+The old DOT graphs remain valuable as heritage: they preserve ideas,
+orientation, roadmap memory, historical design attempts, and inspiration for
+later architecture work.
+
+The immediate operational baseline is the rebuilt 0174 graph draft and its
+subgraphs, combined with the current working code surfaces and the validated
+0162..0174 chain.
+
+## Two-layer graph model
+
+0175 locks a two-layer model:
+
+### Heritage layer
+
+The heritage layer contains older graphs, roadmap notes, stale-doc sections,
+long-term ideas, and orientation traces.
+
+Heritage is useful for inspiration and future design, but it is not the short
+term execution plan.
+
+### Operational baseline layer
+
+The operational baseline layer contains what we can act on now:
+
+- existing runtime bus observation surfaces,
+- existing scheduler route/handler/handshake surfaces,
+- GitHub artifact/dataset chain as currently planned,
+- SQL durable store and Qdrant projection split,
+- ControlProxy/RouteProxy runtime boundaries,
+- 0174 rebuilt graph draft and maintained subgraphs.
+
+This layer is the source for immediate patches.
+
+## Rule for old graphs
+
+Old graphs should not be deleted just because they are stale.
+
+They should be marked, interpreted, and kept as:
+
+```text
+heritage
+orientation
+inspiration
+historical
+stale-doc
+future-idea
+```
+
+They must not silently override the operational baseline.
+
+## Immediate development direction
+
+The next implementation work should not be a graph merge. It should continue
+from the stable base:
+
+```text
+GitHub artifacts / dataset
+-> scheduler-addressable work or bus-compatible observation facts
+-> existing event.bus/context.bus
+-> bus_visualization_adapter
+-> DOT/VisPy/browser projection
+```
+
+and:
+
+```text
+Scheduler / policy / zone
+-> scheduler route handshake / adapter / handler
+-> ControlProxy / RouteProxy when needed
+-> observation facts
+```
+
+## Merge later
+
+A future merge into `00_global.dot` is allowed only later, after the operational
+chain is stable enough and after the old graph has been reviewed as heritage.
+
+The merge must preserve inspirational roadmap content instead of flattening it.
+
+## Non-goals
+
+- No direct `00_global.dot` merge.
+- No graph deletion.
+- No runtime implementation.
+- No Scheduler modification.
+- No VisPy renderer.
+- No new EventBus.
+- No parallel bus.
+- No GitHub API.
+- No network.
+- No conversion execution.
+- No inference.
+- No SQL/qdrant write.
diff --git a/doc/code-rules/0175_graph_heritage_operational_baseline_rule.md b/doc/code-rules/0175_graph_heritage_operational_baseline_rule.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/code-rules/0175_graph_heritage_operational_baseline_rule.md
@@ -0,0 +1,21 @@
+# 0175 graph heritage and operational baseline rule
+
+0175 prevents premature graph merging.
+
+Rules:
+
+- Do not merge or replace `doc/docs/architecture/00_global.dot` in this phase.
+- Keep old DOT graphs as heritage, orientation, historical memory, and
+  inspiration.
+- Mark stale graph areas as `stale-doc` instead of deleting them silently.
+- Treat 0174 rebuilt graph draft and subgraphs as the immediate operational
+  baseline.
+- Immediate patches must follow working code surfaces and the validated
+  0162..0174 chain.
+- Old graphs can inspire future work, but they must not override the operational
+  baseline.
+- Keep GitHub as workflow/exchange/validation surface.
+- Keep EventBus as observation-only.
+- Keep VisPy/browser as read/projection surface.
+- Keep DOT as representation, not runtime authority.
+- Do not create a parallel bus, direct VisPy writer, or Scheduler bypass.
diff --git a/doc/CHANGELOG_0175_GRAPH_HERITAGE_OPERATIONAL_BASELINE.md b/doc/CHANGELOG_0175_GRAPH_HERITAGE_OPERATIONAL_BASELINE.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/CHANGELOG_0175_GRAPH_HERITAGE_OPERATIONAL_BASELINE.md
@@ -0,0 +1,17 @@
+# Changelog — 0175 Graph heritage and operational baseline
+
+## Added
+
+- Rule that old graphs are preserved as heritage/orientation/inspiration.
+- Two-layer model: heritage layer and operational baseline layer.
+- Lock that 0174 rebuilt graph draft is the immediate baseline for short-term
+  development.
+- Rule that `00_global.dot` is not merged or replaced in this phase.
+
+## Not changed
+
+- No runtime code.
+- No Scheduler code.
+- No VisPy renderer.
+- No `00_global.dot` merge.
+- No network, GitHub API, conversion, inference, SQL, or Qdrant execution.
diff --git a/doc/manifests/MANIFEST_0175_CHANGED_FILES.md b/doc/manifests/MANIFEST_0175_CHANGED_FILES.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/manifests/MANIFEST_0175_CHANGED_FILES.md
@@ -0,0 +1,9 @@
+# Manifest — 0175 Graph heritage and operational baseline
+
+- PHASE0175_TEST_REPORT.md
+- doc/architecture/GRAPH_HERITAGE_OPERATIONAL_BASELINE_0175.md
+- doc/code-rules/0175_graph_heritage_operational_baseline_rule.md
+- doc/docs/architecture/runtime/175_graph_heritage_operational_baseline.dot
+- doc/CHANGELOG_0175_GRAPH_HERITAGE_OPERATIONAL_BASELINE.md
+- doc/manifests/MANIFEST_0175_CHANGED_FILES.md
+- tests/rules/test_graph_heritage_operational_baseline_0175_rule.py
diff --git a/doc/docs/architecture/runtime/175_graph_heritage_operational_baseline.dot b/doc/docs/architecture/runtime/175_graph_heritage_operational_baseline.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/175_graph_heritage_operational_baseline.dot
@@ -0,0 +1,48 @@
+digraph graph_heritage_operational_baseline_0175 {
+  rankdir=LR;
+  label="0175 graph heritage and operational baseline";
+  labelloc=t;
+
+  subgraph cluster_heritage {
+    label="Heritage layer";
+    old_global [label="old 00_global.dot\nheritage / stale-doc"];
+    old_subgraphs [label="old subgraphs\norientation / historical"];
+    changelog_holes [label="changelog holes\ndocumentation gap"];
+    future_ideas [label="future ideas\ninspiration"];
+  }
+
+  subgraph cluster_baseline {
+    label="Operational baseline layer";
+    rebuilt_0174 [label="0174 rebuilt graph draft\nimmediate baseline"];
+    code_surfaces [label="working code surfaces"];
+    tests_rules [label="tests / code-rules"];
+    chain [label="validated 0162..0174 chain"];
+  }
+
+  subgraph cluster_immediate_chain {
+    label="Immediate work chain";
+    github [label="GitHub artifacts / dataset"];
+    scheduler_work [label="scheduler-addressable work"];
+    bus_facts [label="event.bus/context.bus facts"];
+    bus_adapter [label="bus_visualization_adapter"];
+    dot_vispy [label="DOT / VisPy/browser projection"];
+  }
+
+  old_global -> future_ideas [label="inspires"];
+  old_subgraphs -> future_ideas [label="inspires"];
+  changelog_holes -> future_ideas [label="context"];
+
+  code_surfaces -> rebuilt_0174 [label="primary evidence"];
+  tests_rules -> rebuilt_0174 [label="guards"];
+  chain -> rebuilt_0174 [label="current direction"];
+
+  rebuilt_0174 -> github;
+  github -> scheduler_work;
+  scheduler_work -> bus_facts [label="facts only"];
+  bus_facts -> bus_adapter [label="read"];
+  bus_adapter -> dot_vispy [label="projection"];
+
+  old_global -> rebuilt_0174 [label="must not override", style=dashed];
+  dot_vispy -> scheduler_work [label="forbidden command path", style=dashed];
+  github -> dot_vispy [label="forbidden direct writer", style=dashed];
+}
diff --git a/tests/rules/test_graph_heritage_operational_baseline_0175_rule.py b/tests/rules/test_graph_heritage_operational_baseline_0175_rule.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/tests/rules/test_graph_heritage_operational_baseline_0175_rule.py
@@ -0,0 +1,90 @@
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+DOC = ROOT / "doc" / "architecture" / "GRAPH_HERITAGE_OPERATIONAL_BASELINE_0175.md"
+RULE = ROOT / "doc" / "code-rules" / "0175_graph_heritage_operational_baseline_rule.md"
+MANIFEST = ROOT / "doc" / "manifests" / "MANIFEST_0175_CHANGED_FILES.md"
+DOT = ROOT / "doc" / "docs" / "architecture" / "runtime" / "175_graph_heritage_operational_baseline.dot"
+
+
+def test_0175_doc_preserves_old_graphs_as_heritage() -> None:
+    doc = DOC.read_text(encoding="utf-8")
+    for token in [
+        "0175 explicitly avoids merging or replacing the old global graph",
+        "old DOT graphs remain valuable as heritage",
+        "ideas, orientation, roadmap memory, historical design attempts, and inspiration",
+        "The immediate operational baseline is the rebuilt 0174 graph draft",
+        "two-layer model",
+        "Heritage layer",
+        "Operational baseline layer",
+    ]:
+        assert token in doc
+
+
+def test_0175_doc_locks_immediate_direction_without_merge() -> None:
+    doc = DOC.read_text(encoding="utf-8")
+    for token in [
+        "Old graphs should not be deleted just because they are stale",
+        "heritage",
+        "orientation",
+        "inspiration",
+        "stale-doc",
+        "future-idea",
+        "must not silently override the operational baseline",
+        "The next implementation work should not be a graph merge",
+        "GitHub artifacts / dataset",
+        "existing event.bus/context.bus",
+        "bus_visualization_adapter",
+        "DOT/VisPy/browser projection",
+    ]:
+        assert token in doc
+
+
+def test_0175_rule_blocks_premature_global_graph_merge() -> None:
+    rule = RULE.read_text(encoding="utf-8")
+    for token in [
+        "Do not merge or replace `doc/docs/architecture/00_global.dot` in this phase",
+        "Keep old DOT graphs as heritage, orientation, historical memory, and inspiration",
+        "Mark stale graph areas as `stale-doc`",
+        "0174 rebuilt graph draft and subgraphs as the immediate operational baseline",
+        "Old graphs can inspire future work",
+        "must not override the operational baseline",
+        "Do not create a parallel bus, direct VisPy writer, or Scheduler bypass",
+    ]:
+        assert token in rule
+
+
+def test_0175_dot_contains_two_layers_and_forbidden_edges() -> None:
+    dot = DOT.read_text(encoding="utf-8")
+    for token in [
+        "0175 graph heritage and operational baseline",
+        "Heritage layer",
+        "Operational baseline layer",
+        "Immediate work chain",
+        "old 00_global.dot",
+        "heritage / stale-doc",
+        "0174 rebuilt graph draft",
+        "immediate baseline",
+        "event.bus/context.bus facts",
+        "bus_visualization_adapter",
+        "must not override",
+        "forbidden command path",
+        "forbidden direct writer",
+    ]:
+        assert token in dot
+
+
+def test_0175_manifest_does_not_include_00_global_dot() -> None:
+    manifest = MANIFEST.read_text(encoding="utf-8")
+    for token in [
+        "PHASE0175_TEST_REPORT.md",
+        "GRAPH_HERITAGE_OPERATIONAL_BASELINE_0175.md",
+        "0175_graph_heritage_operational_baseline_rule.md",
+        "175_graph_heritage_operational_baseline.dot",
+        "test_graph_heritage_operational_baseline_0175_rule.py",
+    ]:
+        assert token in manifest
+
+    assert "doc/docs/architecture/00_global.dot" not in manifest
+    assert "00_global_0175_refresh_candidate.dot" not in manifest
