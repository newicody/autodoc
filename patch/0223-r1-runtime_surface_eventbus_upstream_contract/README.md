# 0223-r1-runtime_surface_eventbus_upstream_contract

Written contract patch for runtime surfaces as upstream EventBus sources for passive supervision.

Apply with:

```bash
python apply_patch_queue.py --patch 0223-r1-runtime_surface_eventbus_upstream_contract --dry-run --allow-dirty
python apply_patch_queue.py --patch 0223-r1-runtime_surface_eventbus_upstream_contract --commit --push --allow-dirty
```
