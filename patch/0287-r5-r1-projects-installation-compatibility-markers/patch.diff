diff --git a/templates/github/projects-repository/INSTALLATION.md b/templates/github/projects-repository/INSTALLATION.md
--- a/templates/github/projects-repository/INSTALLATION.md
+++ b/templates/github/projects-repository/INSTALLATION.md
@@ -1,6 +1,8 @@
-# Installation de `newicody/projects`
-
-Version actuelle du guide : `0287-r5`.
+# Installation cumulative de `newicody/projects`
+
+Version du guide : `0287-r5-r1`.
+
+Ce document est le mode opératoire cumulatif simplifié pour installer et mettre à jour l’interface GitHub du projet.
 
 Ce guide installe uniquement l’interface GitHub du projet : formulaires
 d’Issues, workflow Actions, scripts de projection et configuration ProjectV2.
@@ -77,6 +79,8 @@
 ```
 
 Aucun secret Actions n’est requis par défaut.
+
+Cette valeur est le défaut d'installation. Ne pas créer de secret `AUTODOC_COPILOT_TOKEN` : le workflow actuel utilise uniquement le token éphémère fourni par GitHub.
 
 ## Tokens : lequel utiliser et où le trouver ?
 
@@ -218,6 +222,8 @@
 `GITHUB_TOKEN`, avec les permissions minimales déclarées dans le YAML. Il ne
 nécessite pas `AUTODOC_COPILOT_TOKEN`.
 
+Le workflow ne publie pas lui-même son avis : il produit les artefacts de demande et d’avis consultatif. Toute publication vers une Issue reste soumise à l’adapter contrôlé, au preview, au digest et à la décision opérateur.
+
 Contrôles rapides :
 
 ```bash
@@ -247,6 +253,8 @@
 ```
 
 Vérification sans mutation :
+
+Cette vérification query-only ne déclenche aucun dispatch et ne réalise aucune mutation distante.
 
 ```bash
 PYTHONPATH=src:. python \
diff --git a/tests/rules/test_projects_installation_compatibility_markers_0287_r5_r1_rule.py b/tests/rules/test_projects_installation_compatibility_markers_0287_r5_r1_rule.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/tests/rules/test_projects_installation_compatibility_markers_0287_r5_r1_rule.py
@@ -0,0 +1,42 @@
+from pathlib import Path
+import re
+
+ROOT = Path(__file__).resolve().parents[2]
+INSTALLATION = (
+    ROOT / "templates/github/projects-repository/INSTALLATION.md"
+)
+
+
+def test_simplified_projects_installation_keeps_compatibility_markers() -> None:
+    text = INSTALLATION.read_text(encoding="utf-8")
+
+    for marker in (
+        "Installation cumulative de `newicody/projects`",
+        "mode opératoire cumulatif",
+        "Le workflow ne publie pas lui-même son avis",
+        "Cette valeur est le défaut d'installation",
+        "Ne pas créer de secret `AUTODOC_COPILOT_TOKEN`",
+        "ne déclenche aucun dispatch",
+    ):
+        assert marker in text
+
+    current = re.search(
+        r"^Version du guide : `(?P<version>[^`]+)`\.$",
+        text,
+        flags=re.MULTILINE,
+    )
+    assert current is not None
+    assert current.group("version") == "0287-r5-r1"
+
+    for historical in (
+        "Version du guide : `0286-r4`.",
+        "Version du guide : `0286-r3`.",
+        "Version du guide : `0284-r9`.",
+        "Version du guide : `0284-r1-r5`.",
+    ):
+        assert historical in text
+
+    assert text.index("Version du guide : `0287-r5-r1`.") < text.index(
+        "Version du guide : `0286-r4`."
+    )
+    assert len(text.splitlines()) < 380
diff --git a/PHASE0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS_REPORT.md b/PHASE0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS_REPORT.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/PHASE0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS_REPORT.md
@@ -0,0 +1,18 @@
+# Phase 0287-r5-r1 — Projects installation compatibility markers
+
+Status: corrective documentation patch complete.
+
+The 0287-r5 installation rewrite correctly simplified the operational guide,
+but removed seven exact phrases still asserted by historical architecture and
+safety rules. The simplified structure is retained. The missing compatibility
+statements are restored in their relevant sections:
+
+- cumulative operating procedure;
+- current guide version;
+- safe Copilot default;
+- no durable Copilot secret;
+- advisory publication boundary;
+- query-only verification without dispatch.
+
+No runtime, workflow, ProjectV2 configuration, token requirement or authority
+boundary changes.
diff --git a/doc/CHANGELOG_0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS.md b/doc/CHANGELOG_0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/CHANGELOG_0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS.md
@@ -0,0 +1,9 @@
+# Changelog 0287-r5-r1
+
+- restore the cumulative installation title and exact current-version marker;
+- restore the historical phrase `mode opératoire cumulatif`;
+- state that disabled Copilot advisory is the installation default;
+- state explicitly that `AUTODOC_COPILOT_TOKEN` must not be created;
+- restore the advisory publication boundary wording;
+- state that query-only verification triggers no dispatch;
+- keep the simplified 0287-r5 guide and token procedure.
diff --git a/doc/manifests/MANIFEST_0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS.md b/doc/manifests/MANIFEST_0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/manifests/MANIFEST_0287_R5_R1_PROJECTS_INSTALLATION_COMPATIBILITY_MARKERS.md
@@ -0,0 +1,12 @@
+# Manifest 0287-r5-r1
+
+Modified:
+
+- `templates/github/projects-repository/INSTALLATION.md`
+
+Added:
+
+- `tests/rules/test_projects_installation_compatibility_markers_0287_r5_r1_rule.py`
+- phase corrective report
+- changelog
+- this manifest
