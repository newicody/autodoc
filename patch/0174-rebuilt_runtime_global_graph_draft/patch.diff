diff --git a/PHASE0174_TEST_REPORT.md b/PHASE0174_TEST_REPORT.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/PHASE0174_TEST_REPORT.md
@@ -0,0 +1,20 @@
+# Phase 0174 Test Report — Rebuilt runtime global graph draft
+
+Status: prepared.
+
+Scope:
+- Documentation/graph reconstruction draft only.
+- Does not rewrite `doc/docs/architecture/00_global.dot`.
+- Adds a current-state global draft graph and maintained subgraph drafts.
+- Deduces the graph from existing code surfaces, passing rule intent, and the
+  0162..0173 chain.
+- Marks stale or planned areas explicitly.
+
+Validation:
+
+```bash
+python -m compileall -q src tests tools
+python -m pytest -q tests/rules/test_rebuilt_runtime_global_graph_draft_0174_rule.py
+python -m pytest -q tests/rules
+python -m pytest -q
+```
diff --git a/doc/architecture/REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT_0174.md b/doc/architecture/REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT_0174.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/architecture/REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT_0174.md
@@ -0,0 +1,137 @@
+# 0174 — Rebuilt runtime global graph draft
+
+## Decision
+
+0174 adds a rebuilt current-state graph draft and subgraph drafts. It does not
+replace `doc/docs/architecture/00_global.dot`.
+
+The goal is to build a fresher representation from:
+
+- code surfaces that currently exist,
+- tests/rules/manifests that guard those surfaces,
+- the validated 0162..0173 chain,
+- stale docs and changelogs only as historical context.
+
+This is a current-state draft, not runtime authority.
+
+## Why not rewrite 00_global.dot yet
+
+`00_global.dot` is old but valuable. It carries accumulated roadmap and history.
+A direct rewrite would risk deleting context that helps humans and AI agents.
+
+0174 therefore creates a parallel draft:
+
+```text
+doc/docs/architecture/runtime/174_rebuilt_runtime_global_current_state.dot
+```
+
+and explicit subgraphs under:
+
+```text
+doc/docs/architecture/runtime/0174_subgraphs/
+```
+
+A later patch may merge this into `00_global.dot` after review.
+
+## Current-state global graph
+
+The rebuilt draft represents the chain we are actually converging on:
+
+```text
+GitHub Project / issue / idea repo
+-> GitHub Actions ticket artifact
+-> Copilot preliminary opinion artifact
+-> read-only artifact fetch
+-> configured server dataset
+-> raw/index/history/queue
+-> scheduler-addressable work or bus-compatible observation facts
+-> Scheduler / policy / zone
+-> scheduler route handshake / adapter / handler
+-> ControlProxy / RouteProxy runtime data plane when needed
+-> OpenVINO / E5 / specialist work when scheduled
+-> Qdrant projection/search
+-> SQL durable store / rehydrate
+-> review/publication artifact
+-> GitHub validation surface
+```
+
+Observation is represented separately:
+
+```text
+runtime facts
+-> event.bus/context.bus
+-> bus_visualization_adapter
+-> DOT activity graph support
+-> VisPy/browser view
+```
+
+## Required subgraph drafts
+
+0174 adds these draft subgraphs:
+
+- `runtime-bus`
+- `scheduler-route`
+- `github-artifact-dataset`
+- `vector-sql-qdrant`
+- `controlproxy-routeproxy`
+- `activity-graph-vispy`
+- `docs-provenance`
+
+They are designed as maintained subgraphs that can later feed the refreshed
+global graph and the VisPy/browser view modes.
+
+## Freshness status rules
+
+Nodes and edges use status language from the active architecture vocabulary:
+
+- `current`
+- `validated`
+- `partial`
+- `planned`
+- `historical`
+- `superseded`
+- `deprecated`
+- `blocked`
+- `stale-doc`
+
+Do not use `abandoned` unless it is explicitly proven.
+
+## Runtime authority rule
+
+DOT and VisPy/browser are representation surfaces. They must not become command
+paths.
+
+Runtime authority remains with:
+
+- Scheduler / policy / zone for orchestration and authorization,
+- SQL/local dataset for durable authority where applicable,
+- Qdrant only as projection/search,
+- EventBus only as observation.
+
+## Merge policy for later 00_global.dot refresh
+
+A later merge into `00_global.dot` must:
+
+- inspect the exact local `00_global.dot`,
+- preserve useful historical roadmap notes,
+- import reviewed current-state clusters from 0174,
+- link to maintained subgraph files,
+- mark stale areas explicitly,
+- avoid deleting changelog holes silently,
+- avoid claiming execution that does not exist,
+- avoid turning DOT into runtime authority.
+
+## Non-goals
+
+- No runtime code.
+- No VisPy renderer.
+- No Scheduler modification.
+- No new EventBus.
+- No parallel bus.
+- No direct VisPy writer.
+- No GitHub API.
+- No network.
+- No conversion execution.
+- No inference.
+- No SQL/qdrant write.
+- No direct `00_global.dot` rewrite.
diff --git a/doc/code-rules/0174_rebuilt_runtime_global_graph_draft_rule.md b/doc/code-rules/0174_rebuilt_runtime_global_graph_draft_rule.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/code-rules/0174_rebuilt_runtime_global_graph_draft_rule.md
@@ -0,0 +1,30 @@
+# 0174 rebuilt runtime global graph draft rule
+
+0174 creates a current-state graph draft and subgraph drafts without replacing
+`doc/docs/architecture/00_global.dot`.
+
+Rules:
+
+- Treat 0174 graphs as representation drafts, not runtime authority.
+- Do not replace `00_global.dot` in this patch.
+- Deduce the graph from code surfaces, tests/rules/manifests, and the validated
+  0162..0173 chain.
+- Treat stale docs and changelogs as historical context, not sole truth.
+- Mark stale areas with `stale-doc`.
+- Keep GitHub as workflow/exchange/validation surface.
+- Keep configured server dataset and SQL/local stores as authority surfaces.
+- Keep Qdrant as projection/search, not durable authority.
+- Keep EventBus as observation-only.
+- Keep VisPy/browser as a read/projection surface.
+- Do not create a parallel bus, direct VisPy writer, or Scheduler bypass.
+
+Required graph files:
+
+- `doc/docs/architecture/runtime/174_rebuilt_runtime_global_current_state.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/runtime_bus.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/scheduler_route.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/github_artifact_dataset.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/vector_sql_qdrant.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/controlproxy_routeproxy.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/activity_graph_vispy.dot`
+- `doc/docs/architecture/runtime/0174_subgraphs/docs_provenance.dot`
diff --git a/doc/CHANGELOG_0174_REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT.md b/doc/CHANGELOG_0174_REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/CHANGELOG_0174_REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT.md
@@ -0,0 +1,17 @@
+# Changelog — 0174 Rebuilt runtime global graph draft
+
+## Added
+
+- Current-state global graph draft derived from working architecture direction.
+- Maintained subgraph drafts for runtime bus, scheduler route, GitHub artifact
+  dataset, vector/SQL/Qdrant, ControlProxy/RouteProxy, activity graph VisPy,
+  and docs provenance.
+- Rule that this patch must not rewrite `00_global.dot`.
+
+## Not changed
+
+- No runtime code.
+- No Scheduler code.
+- No VisPy renderer.
+- No `00_global.dot` rewrite.
+- No network, GitHub API, conversion, inference, SQL, or Qdrant execution.
diff --git a/doc/manifests/MANIFEST_0174_CHANGED_FILES.md b/doc/manifests/MANIFEST_0174_CHANGED_FILES.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/manifests/MANIFEST_0174_CHANGED_FILES.md
@@ -0,0 +1,16 @@
+# Manifest — 0174 Rebuilt runtime global graph draft
+
+- PHASE0174_TEST_REPORT.md
+- doc/architecture/REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT_0174.md
+- doc/code-rules/0174_rebuilt_runtime_global_graph_draft_rule.md
+- doc/docs/architecture/runtime/174_rebuilt_runtime_global_current_state.dot
+- doc/docs/architecture/runtime/0174_subgraphs/runtime_bus.dot
+- doc/docs/architecture/runtime/0174_subgraphs/scheduler_route.dot
+- doc/docs/architecture/runtime/0174_subgraphs/github_artifact_dataset.dot
+- doc/docs/architecture/runtime/0174_subgraphs/vector_sql_qdrant.dot
+- doc/docs/architecture/runtime/0174_subgraphs/controlproxy_routeproxy.dot
+- doc/docs/architecture/runtime/0174_subgraphs/activity_graph_vispy.dot
+- doc/docs/architecture/runtime/0174_subgraphs/docs_provenance.dot
+- doc/CHANGELOG_0174_REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT.md
+- doc/manifests/MANIFEST_0174_CHANGED_FILES.md
+- tests/rules/test_rebuilt_runtime_global_graph_draft_0174_rule.py
diff --git a/doc/docs/architecture/runtime/174_rebuilt_runtime_global_current_state.dot b/doc/docs/architecture/runtime/174_rebuilt_runtime_global_current_state.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/174_rebuilt_runtime_global_current_state.dot
@@ -0,0 +1,76 @@
+digraph rebuilt_runtime_global_current_state_0174 {
+  rankdir=LR;
+  label="0174 rebuilt runtime global current-state draft";
+  labelloc=t;
+
+  subgraph cluster_external {
+    label="External workflow / exchange";
+    github_project [label="GitHub Project / issue / idea repo\nstatus=current"];
+    ticket_artifact [label="GitHub Action ticket artifact\nstatus=validated"];
+    copilot_opinion [label="Copilot preliminary opinion artifact\nstatus=planned"];
+    github_review [label="GitHub review / validation surface\nstatus=planned"];
+  }
+
+  subgraph cluster_dataset {
+    label="Configured server dataset";
+    fetch [label="read-only artifact fetch\nstatus=partial"];
+    raw [label="raw artifacts / attachments\nstatus=planned"];
+    index [label="index / history\nstatus=partial"];
+    queue [label="conversion or scheduler queue\nstatus=planned"];
+  }
+
+  subgraph cluster_scheduler {
+    label="Scheduler authority";
+    scheduler [label="Scheduler / policy / zone\nstatus=current"];
+    route_adapter [label="scheduler route handshake / adapter / handler\nstatus=current"];
+    controlproxy [label="ControlProxy / RouteProxy data plane\nstatus=current"];
+  }
+
+  subgraph cluster_compute {
+    label="Local compute / context";
+    openvino [label="OpenVINO / E5 / specialists\nstatus=partial"];
+    qdrant [label="Qdrant projection/search\nstatus=validated"];
+    sql [label="SQL durable store / rehydrate\nstatus=validated"];
+    result_artifact [label="review/publication artifact\nstatus=planned"];
+  }
+
+  subgraph cluster_observation {
+    label="Observation / visualization";
+    event_bus [label="event.bus facts\nstatus=current"];
+    context_bus [label="context.bus facts\nstatus=current"];
+    bus_adapter [label="bus_visualization_adapter\nstatus=current"];
+    dot_support [label="DOT activity graph support\nstatus=validated"];
+    vispy [label="VisPy/browser view\nstatus=partial"];
+  }
+
+  github_project -> ticket_artifact;
+  ticket_artifact -> copilot_opinion;
+  copilot_opinion -> fetch;
+  fetch -> raw;
+  raw -> index;
+  index -> queue;
+  queue -> scheduler;
+  scheduler -> route_adapter;
+  route_adapter -> controlproxy;
+  scheduler -> openvino [label="when scheduled"];
+  openvino -> qdrant;
+  qdrant -> sql [label="sql_ref rehydrate"];
+  sql -> result_artifact;
+  result_artifact -> github_review;
+
+  route_adapter -> event_bus [label="facts only"];
+  route_adapter -> context_bus [label="facts only"];
+  controlproxy -> event_bus [label="facts only"];
+  controlproxy -> context_bus [label="facts only"];
+  fetch -> event_bus [label="future facts only", style=dashed];
+  queue -> event_bus [label="future facts only", style=dashed];
+
+  event_bus -> bus_adapter [label="read"];
+  context_bus -> bus_adapter [label="snapshot"];
+  bus_adapter -> dot_support [label="projection"];
+  dot_support -> vispy [label="titles / zoom / modes"];
+
+  dot_support -> scheduler [label="forbidden command path", style=dashed];
+  fetch -> vispy [label="forbidden direct writer", style=dashed];
+  github_project -> event_bus [label="forbidden internal bus source", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/runtime_bus.dot b/doc/docs/architecture/runtime/0174_subgraphs/runtime_bus.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/runtime_bus.dot
@@ -0,0 +1,21 @@
+digraph runtime_bus_0174 {
+  rankdir=LR;
+  label="0174 subgraph: runtime-bus";
+  labelloc=t;
+
+  shm_schema [label="runtime.shm_runtime_schema\nstatus=current"];
+  event_bus [label="event.bus facts\nobservation-only"];
+  context_bus [label="context.bus facts\nobservation-only"];
+  bus_adapter [label="bus_visualization_adapter\nread existing bus"];
+  snapshot [label="visualization snapshot\nstatus=current"];
+  vispy [label="VisPy/browser projection\nstatus=partial"];
+
+  shm_schema -> event_bus [label="EVENT_BUS_MESSAGE_SCHEMA"];
+  shm_schema -> context_bus [label="CONTEXT_BUS_MESSAGE_SCHEMA"];
+  event_bus -> bus_adapter [label="event_bus.subscribe()"];
+  context_bus -> bus_adapter [label="context snapshot source"];
+  bus_adapter -> snapshot;
+  snapshot -> vispy;
+
+  vispy -> event_bus [label="forbidden writer", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/scheduler_route.dot b/doc/docs/architecture/runtime/0174_subgraphs/scheduler_route.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/scheduler_route.dot
@@ -0,0 +1,24 @@
+digraph scheduler_route_0174 {
+  rankdir=LR;
+  label="0174 subgraph: scheduler-route";
+  labelloc=t;
+
+  scheduler [label="Scheduler / policy / zone\nauthority"];
+  request [label="SchedulerRouteRequest\nauthorized=True + policy_decision_id"];
+  handshake [label="prepare_route_for_scheduler(...)"];
+  adapter [label="handle_scheduler_route_request(...)"];
+  handler [label="SchedulerRouteHandlerCommand"];
+  route_runtime [label="RouteProxyRuntime frame IO"];
+  bus [label="event.bus/context.bus facts\nobservation-only"];
+
+  scheduler -> request;
+  request -> handshake;
+  handshake -> adapter;
+  adapter -> handler;
+  handler -> route_runtime;
+  adapter -> bus [label="adapter facts"];
+  handshake -> bus [label="handshake facts"];
+  route_runtime -> bus [label="runtime facts"];
+
+  bus -> scheduler [label="forbidden command path", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/github_artifact_dataset.dot b/doc/docs/architecture/runtime/0174_subgraphs/github_artifact_dataset.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/github_artifact_dataset.dot
@@ -0,0 +1,30 @@
+digraph github_artifact_dataset_0174 {
+  rankdir=LR;
+  label="0174 subgraph: github-artifact-dataset";
+  labelloc=t;
+
+  project [label="GitHub Project / issue / idea repo\nworkflow surface"];
+  action [label="GitHub Action"];
+  ticket [label="ticket artifact"];
+  opinion [label="Copilot preliminary opinion artifact\nplanned"];
+  fetch [label="read-only artifact fetch\npartial"];
+  dataset [label="configured server dataset\nlocal/server surface"];
+  raw [label="raw"];
+  index [label="index/history"];
+  queue [label="queue / scheduler-addressable work"];
+  bus_fact [label="future bus-compatible observation facts"];
+
+  project -> action;
+  action -> ticket;
+  action -> opinion;
+  ticket -> fetch;
+  opinion -> fetch;
+  fetch -> dataset;
+  dataset -> raw;
+  dataset -> index;
+  dataset -> queue;
+  queue -> bus_fact [label="facts only / future bridge"];
+
+  fetch -> project [label="forbidden mutation in fetch path", style=dashed];
+  dataset -> project [label="publication later only", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/vector_sql_qdrant.dot b/doc/docs/architecture/runtime/0174_subgraphs/vector_sql_qdrant.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/vector_sql_qdrant.dot
@@ -0,0 +1,22 @@
+digraph vector_sql_qdrant_0174 {
+  rankdir=LR;
+  label="0174 subgraph: vector-sql-qdrant";
+  labelloc=t;
+
+  source [label="durable source / SQL record"];
+  embed [label="OpenVINO / E5 embedding\nquery/passages"];
+  qdrant [label="Qdrant projection/search"];
+  hit [label="vector hit + sql_ref"];
+  sql [label="SQL durable hydration"];
+  context [label="hydrated context"];
+  result [label="review / specialist result"];
+
+  source -> embed;
+  embed -> qdrant;
+  qdrant -> hit;
+  hit -> sql [label="sql_ref"];
+  sql -> context;
+  context -> result;
+
+  qdrant -> source [label="forbidden durable authority", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/controlproxy_routeproxy.dot b/doc/docs/architecture/runtime/0174_subgraphs/controlproxy_routeproxy.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/controlproxy_routeproxy.dot
@@ -0,0 +1,24 @@
+digraph controlproxy_routeproxy_0174 {
+  rankdir=LR;
+  label="0174 subgraph: controlproxy-routeproxy";
+  labelloc=t;
+
+  scheduler [label="Scheduler-approved route demand"];
+  controlproxy [label="ControlProxy\nfast gate / leases / hints"];
+  routeproxy [label="RouteProxyRuntime\nframes / permits"];
+  shm [label="/dev/shm or explicit runtime root"];
+  frames [label="route frames"];
+  bus [label="event.bus/context.bus facts"];
+  policy [label="policy/zone authority"];
+
+  policy -> scheduler;
+  scheduler -> controlproxy;
+  controlproxy -> routeproxy;
+  routeproxy -> shm;
+  shm -> frames;
+  controlproxy -> bus [label="facts"];
+  routeproxy -> bus [label="facts"];
+
+  controlproxy -> policy [label="forbidden policy decision", style=dashed];
+  bus -> scheduler [label="forbidden command path", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/activity_graph_vispy.dot b/doc/docs/architecture/runtime/0174_subgraphs/activity_graph_vispy.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/activity_graph_vispy.dot
@@ -0,0 +1,25 @@
+digraph activity_graph_vispy_0174 {
+  rankdir=LR;
+  label="0174 subgraph: activity-graph-vispy";
+  labelloc=t;
+
+  bus_snapshot [label="bus visualization snapshot"];
+  dot_support [label="DOT activity graph support"];
+  title [label="titles / labels"];
+  navigation [label="navigation / zoom / pan"];
+  modes [label="view modes"];
+  states [label="states / score / population"];
+  vispy [label="VisPy/browser"];
+
+  bus_snapshot -> dot_support;
+  dot_support -> title;
+  dot_support -> navigation;
+  dot_support -> modes;
+  dot_support -> states;
+  title -> vispy;
+  navigation -> vispy;
+  modes -> vispy;
+  states -> vispy;
+
+  dot_support -> bus_snapshot [label="forbidden command feedback", style=dashed];
+}
diff --git a/doc/docs/architecture/runtime/0174_subgraphs/docs_provenance.dot b/doc/docs/architecture/runtime/0174_subgraphs/docs_provenance.dot
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/doc/docs/architecture/runtime/0174_subgraphs/docs_provenance.dot
@@ -0,0 +1,23 @@
+digraph docs_provenance_0174 {
+  rankdir=LR;
+  label="0174 subgraph: docs-provenance";
+  labelloc=t;
+
+  code [label="working code surfaces"];
+  tests [label="tests / rules"];
+  manifests [label="manifests"];
+  patch_queue [label="patch queue traces"];
+  changelogs [label="changelogs\nmay have holes"];
+  old_graphs [label="old DOT graphs\nstale-doc possible"];
+  rebuilt [label="0174 rebuilt graph draft"];
+
+  code -> rebuilt [label="primary"];
+  tests -> rebuilt [label="guards"];
+  manifests -> rebuilt [label="changed-file evidence"];
+  patch_queue -> rebuilt [label="development trace"];
+  changelogs -> rebuilt [label="context"];
+  old_graphs -> rebuilt [label="historical input"];
+
+  old_graphs -> code [label="forbidden sole truth", style=dashed];
+  changelogs -> code [label="forbidden absence proof", style=dashed];
+}
diff --git a/tests/rules/test_rebuilt_runtime_global_graph_draft_0174_rule.py b/tests/rules/test_rebuilt_runtime_global_graph_draft_0174_rule.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/tests/rules/test_rebuilt_runtime_global_graph_draft_0174_rule.py
@@ -0,0 +1,119 @@
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+DOC = ROOT / "doc" / "architecture" / "REBUILT_RUNTIME_GLOBAL_GRAPH_DRAFT_0174.md"
+RULE = ROOT / "doc" / "code-rules" / "0174_rebuilt_runtime_global_graph_draft_rule.md"
+MANIFEST = ROOT / "doc" / "manifests" / "MANIFEST_0174_CHANGED_FILES.md"
+GLOBAL = ROOT / "doc" / "docs" / "architecture" / "runtime" / "174_rebuilt_runtime_global_current_state.dot"
+SUBGRAPH_DIR = ROOT / "doc" / "docs" / "architecture" / "runtime" / "0174_subgraphs"
+
+
+REQUIRED_SUBGRAPHS = (
+    "runtime_bus.dot",
+    "scheduler_route.dot",
+    "github_artifact_dataset.dot",
+    "vector_sql_qdrant.dot",
+    "controlproxy_routeproxy.dot",
+    "activity_graph_vispy.dot",
+    "docs_provenance.dot",
+)
+
+
+def test_0174_doc_locks_parallel_draft_not_global_rewrite() -> None:
+    doc = DOC.read_text(encoding="utf-8")
+    for token in [
+        "0174 adds a rebuilt current-state graph draft",
+        "does not replace `doc/docs/architecture/00_global.dot`",
+        "current-state draft, not runtime authority",
+        "code surfaces that currently exist",
+        "validated 0162..0173 chain",
+        "stale docs and changelogs only as historical context",
+        "A later patch may merge this into `00_global.dot` after review",
+    ]:
+        assert token in doc
+
+
+def test_0174_doc_contains_canonical_chain_and_observation_chain() -> None:
+    doc = DOC.read_text(encoding="utf-8")
+    for token in [
+        "GitHub Project / issue / idea repo",
+        "GitHub Actions ticket artifact",
+        "Copilot preliminary opinion artifact",
+        "read-only artifact fetch",
+        "configured server dataset",
+        "Scheduler / policy / zone",
+        "scheduler route handshake / adapter / handler",
+        "ControlProxy / RouteProxy runtime data plane",
+        "OpenVINO / E5 / specialist work",
+        "Qdrant projection/search",
+        "SQL durable store / rehydrate",
+        "runtime facts",
+        "event.bus/context.bus",
+        "bus_visualization_adapter",
+        "DOT activity graph support",
+        "VisPy/browser view",
+    ]:
+        assert token in doc
+
+
+def test_0174_rule_locks_no_parallel_bus_or_vispy_writer() -> None:
+    rule = RULE.read_text(encoding="utf-8")
+    for token in [
+        "Treat 0174 graphs as representation drafts, not runtime authority",
+        "Do not replace `00_global.dot` in this patch",
+        "Keep GitHub as workflow/exchange/validation surface",
+        "Keep Qdrant as projection/search, not durable authority",
+        "Keep EventBus as observation-only",
+        "Keep VisPy/browser as a read/projection surface",
+        "Do not create a parallel bus, direct VisPy writer, or Scheduler bypass",
+    ]:
+        assert token in rule
+
+
+def test_0174_global_draft_contains_required_clusters_and_forbidden_edges() -> None:
+    graph = GLOBAL.read_text(encoding="utf-8")
+    for token in [
+        "0174 rebuilt runtime global current-state draft",
+        "External workflow / exchange",
+        "Configured server dataset",
+        "Scheduler authority",
+        "Local compute / context",
+        "Observation / visualization",
+        "forbidden command path",
+        "forbidden direct writer",
+        "forbidden internal bus source",
+    ]:
+        assert token in graph
+
+
+def test_0174_subgraph_files_exist_and_have_labels() -> None:
+    for name in REQUIRED_SUBGRAPHS:
+        path = SUBGRAPH_DIR / name
+        assert path.exists(), name
+        text = path.read_text(encoding="utf-8")
+        assert "0174 subgraph:" in text
+        assert "digraph" in text
+
+
+def test_0174_manifest_lists_graph_files_but_not_00_global_dot() -> None:
+    manifest = MANIFEST.read_text(encoding="utf-8")
+    for name in REQUIRED_SUBGRAPHS:
+        assert name in manifest
+    assert "174_rebuilt_runtime_global_current_state.dot" in manifest
+    assert "doc/docs/architecture/00_global.dot" not in manifest
+
+
+def test_0174_docs_provenance_marks_old_graphs_as_historical_input() -> None:
+    provenance = (SUBGRAPH_DIR / "docs_provenance.dot").read_text(encoding="utf-8")
+    for token in [
+        "working code surfaces",
+        "tests / rules",
+        "patch queue traces",
+        "changelogs",
+        "old DOT graphs",
+        "stale-doc possible",
+        "forbidden sole truth",
+        "forbidden absence proof",
+    ]:
+        assert token in provenance
