# 0166 — GitHub Action ticket artifact contract

Apply:

```text
tar -xJf autodoc_patch_0166-github_action_ticket_artifact_contract.tar.xz
python apply_patch_queue.py --patch 0166-github_action_ticket_artifact_contract --dry-run --allow-dirty
python apply_patch_queue.py --patch 0166-github_action_ticket_artifact_contract --commit --push --allow-dirty
```
