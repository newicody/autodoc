# 0273-r1 — Laboratory framework reuse audit

Documentation-only audit before laboratory contracts.

```bash
python apply_patch_queue.py \
  --patch 0273-r1-laboratory_framework_reuse_audit \
  --dry-run \
  --allow-dirty
```

No runtime, provider, manager, registry, Scheduler, bus, backend or dependency is added.
