diff --git a/src/context/source_candidate_operator_external_review_report.py b/src/context/source_candidate_operator_external_review_report.py
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/src/context/source_candidate_operator_external_review_report.py
@@ -0,0 +1,312 @@
+from __future__ import annotations
+
+from dataclasses import dataclass
+from pathlib import Path
+from typing import Any, Mapping, Sequence
+import json
+import os
+import tempfile
+
+
+_REPORT_SCHEMA = "missipy.source_candidate.operator_external_review_report.v1"
+_BUNDLE_SCHEMA = "missipy.source_candidate.github_export_bundle.v1"
+
+
+@dataclass(frozen=True)
+class SourceCandidateOperatorExternalReviewFinding:
+    severity: str
+    code: str
+    message: str
+
+    def to_json_dict(self) -> dict[str, object]:
+        return {
+            "severity": self.severity,
+            "code": self.code,
+            "message": self.message,
+        }
+
+
+@dataclass(frozen=True)
+class SourceCandidateOperatorExternalReviewReport:
+    bundle_path: Path
+    repository: str | None
+    dry_run: bool
+    mutation_allowed: bool
+    operation_count: int
+    artifact_count: int
+    finding_count: int
+    recommended_action: str
+    findings: tuple[SourceCandidateOperatorExternalReviewFinding, ...]
+
+    def to_json_dict(self) -> dict[str, object]:
+        return {
+            "schema": _REPORT_SCHEMA,
+            "bundle_path": str(self.bundle_path),
+            "repository": self.repository,
+            "dry_run": self.dry_run,
+            "mutation_allowed": self.mutation_allowed,
+            "operation_count": self.operation_count,
+            "artifact_count": self.artifact_count,
+            "finding_count": self.finding_count,
+            "recommended_action": self.recommended_action,
+            "findings": [finding.to_json_dict() for finding in self.findings],
+        }
+
+
+def build_source_candidate_operator_external_review_report(
+    bundle_path: Path,
+) -> SourceCandidateOperatorExternalReviewReport:
+    """Build an operator-facing report from a local GitHub export bundle.
+
+    This report is read-only. It inspects local artifacts generated by earlier
+    phases and gives the operator a compact decision summary.
+    """
+
+    manifest_path = bundle_path / "manifest.json"
+    manifest = _read_json_object(manifest_path)
+    if manifest.get("schema") != _BUNDLE_SCHEMA:
+        raise ValueError("GitHub export bundle manifest schema mismatch")
+
+    findings: list[SourceCandidateOperatorExternalReviewFinding] = []
+    artifacts = _artifact_map(manifest)
+
+    _require_artifact(artifacts, "external_projection_contract", findings)
+    _require_artifact(artifacts, "github_projection_payload", findings)
+    gate_artifact = _require_artifact(artifacts, "remote_mutation_gate", findings)
+    adapter_artifact = _require_artifact(artifacts, "github_adapter_dry_run", findings)
+
+    gate_payload = _read_artifact_payload(bundle_path, gate_artifact)
+    adapter_payload = _read_artifact_payload(bundle_path, adapter_artifact)
+
+    dry_run = bool(manifest.get("dry_run"))
+    mutation_allowed = bool(manifest.get("mutation_allowed"))
+    operation_count = _int(manifest.get("operation_count"), default=0)
+    artifact_count = _int(manifest.get("artifact_count"), default=0)
+    repository = _optional_string(manifest.get("repository"))
+
+    if not dry_run:
+        findings.append(
+            _error("payload_not_dry_run", "export bundle is not marked as dry-run")
+        )
+
+    if operation_count <= 0:
+        findings.append(
+            _warning("no_operations", "export bundle contains no external operations")
+        )
+
+    findings.extend(_gate_findings(gate_payload))
+    findings.extend(_adapter_findings(adapter_payload))
+
+    recommended_action = _recommended_action(
+        dry_run=dry_run,
+        mutation_allowed=mutation_allowed,
+        findings=tuple(findings),
+    )
+
+    return SourceCandidateOperatorExternalReviewReport(
+        bundle_path=bundle_path,
+        repository=repository,
+        dry_run=dry_run,
+        mutation_allowed=mutation_allowed,
+        operation_count=operation_count,
+        artifact_count=artifact_count,
+        finding_count=len(findings),
+        recommended_action=recommended_action,
+        findings=tuple(findings),
+    )
+
+
+def write_source_candidate_operator_external_review_report(
+    path: Path,
+    report: SourceCandidateOperatorExternalReviewReport,
+) -> Path:
+    _atomic_write_json(path, report.to_json_dict())
+    return path
+
+
+def render_source_candidate_operator_external_review_report(
+    report: SourceCandidateOperatorExternalReviewReport,
+) -> str:
+    status = "READY" if report.recommended_action == "operator_review" else "BLOCKED"
+    lines = [
+        f"operator external review: {status}",
+        f"repository: {report.repository or '<none>'}",
+        f"bundle: {report.bundle_path}",
+        f"dry_run: {report.dry_run}",
+        f"mutation_allowed: {report.mutation_allowed}",
+        f"operations: {report.operation_count}",
+        f"artifacts: {report.artifact_count}",
+        f"recommended_action: {report.recommended_action}",
+        f"findings: {report.finding_count}",
+    ]
+    for finding in report.findings:
+        lines.append(f"- {finding.severity}:{finding.code}: {finding.message}")
+    return "\n".join(lines)
+
+
+def read_source_candidate_operator_external_review_report(path: Path) -> dict[str, Any]:
+    payload = _read_json_object(path)
+    if payload.get("schema") != _REPORT_SCHEMA:
+        raise ValueError("operator external review report schema mismatch")
+    return dict(payload)
+
+
+def _artifact_map(manifest: Mapping[str, Any]) -> dict[str, Mapping[str, Any]]:
+    raw_artifacts = manifest.get("artifacts")
+    if not isinstance(raw_artifacts, Sequence) or isinstance(raw_artifacts, (str, bytes, bytearray)):
+        raise ValueError("bundle manifest artifacts must be a list")
+
+    artifacts: dict[str, Mapping[str, Any]] = {}
+    for raw_artifact in raw_artifacts:
+        if not isinstance(raw_artifact, Mapping):
+            raise ValueError("bundle manifest artifact must be an object")
+        role = _optional_string(raw_artifact.get("role"))
+        if role is not None:
+            artifacts[role] = raw_artifact
+    return artifacts
+
+
+def _require_artifact(
+    artifacts: Mapping[str, Mapping[str, Any]],
+    role: str,
+    findings: list[SourceCandidateOperatorExternalReviewFinding],
+) -> Mapping[str, Any] | None:
+    artifact = artifacts.get(role)
+    if artifact is None:
+        findings.append(_error("missing_artifact", f"missing bundle artifact: {role}"))
+        return None
+    return artifact
+
+
+def _read_artifact_payload(
+    bundle_path: Path,
+    artifact: Mapping[str, Any] | None,
+) -> Mapping[str, Any]:
+    if artifact is None:
+        return {}
+    path_value = _optional_string(artifact.get("path"))
+    if path_value is None:
+        return {}
+
+    path = Path(path_value)
+    if not path.is_absolute():
+        path = bundle_path / path.name
+    if not path.exists():
+        return {}
+
+    try:
+        return _read_json_object(path)
+    except (json.JSONDecodeError, ValueError):
+        return {}
+
+
+def _gate_findings(payload: Mapping[str, Any]) -> tuple[SourceCandidateOperatorExternalReviewFinding, ...]:
+    findings: list[SourceCandidateOperatorExternalReviewFinding] = []
+    if not payload:
+        return (_error("gate_missing_or_unreadable", "remote mutation gate artifact is missing or unreadable"),)
+
+    mutation_allowed = bool(payload.get("mutation_allowed"))
+    if not mutation_allowed:
+        findings.append(
+            _warning("gate_not_passing", "remote mutation gate is not passing")
+        )
+
+    for issue in _issues(payload):
+        code = _optional_string(issue.get("code")) or "gate_issue"
+        message = _optional_string(issue.get("message")) or "remote mutation gate issue"
+        severity = _optional_string(issue.get("severity")) or "warning"
+        findings.append(SourceCandidateOperatorExternalReviewFinding(severity=severity, code=code, message=message))
+
+    return tuple(findings)
+
+
+def _adapter_findings(payload: Mapping[str, Any]) -> tuple[SourceCandidateOperatorExternalReviewFinding, ...]:
+    if not payload:
+        return (_error("adapter_missing_or_unreadable", "adapter dry-run artifact is missing or unreadable"),)
+
+    gate = payload.get("gate")
+    plan = payload.get("plan")
+    findings: list[SourceCandidateOperatorExternalReviewFinding] = []
+
+    if not isinstance(gate, Mapping):
+        findings.append(_error("adapter_gate_missing", "adapter dry-run does not include gate result"))
+    if not isinstance(plan, Mapping):
+        findings.append(_error("adapter_plan_missing", "adapter dry-run does not include plan"))
+
+    if isinstance(plan, Mapping) and bool(plan.get("remote_mutation")):
+        findings.append(_error("adapter_plan_remote_mutation", "adapter plan is marked as remote mutation"))
+
+    return tuple(findings)
+
+
+def _issues(payload: Mapping[str, Any]) -> tuple[Mapping[str, Any], ...]:
+    raw_issues = payload.get("issues")
+    if not isinstance(raw_issues, Sequence) or isinstance(raw_issues, (str, bytes, bytearray)):
+        return ()
+    return tuple(item for item in raw_issues if isinstance(item, Mapping))
+
+
+def _recommended_action(
+    *,
+    dry_run: bool,
+    mutation_allowed: bool,
+    findings: tuple[SourceCandidateOperatorExternalReviewFinding, ...],
+) -> str:
+    if not dry_run:
+        return "stop_not_dry_run"
+    if any(finding.severity == "error" for finding in findings):
+        return "fix_errors"
+    if not mutation_allowed:
+        return "review_gate_blockers"
+    return "operator_review"
+
+
+def _read_json_object(path: Path) -> Mapping[str, Any]:
+    payload = json.loads(path.read_text(encoding="utf-8"))
+    if not isinstance(payload, Mapping):
+        raise ValueError(f"JSON payload must be an object: {path}")
+    return payload
+
+
+def _optional_string(raw: object) -> str | None:
+    if isinstance(raw, str) and raw.strip():
+        return raw
+    return None
+
+
+def _int(raw: object, *, default: int) -> int:
+    if isinstance(raw, int):
+        return raw
+    return default
+
+
+def _error(code: str, message: str) -> SourceCandidateOperatorExternalReviewFinding:
+    return SourceCandidateOperatorExternalReviewFinding(
+        severity="error",
+        code=code,
+        message=message,
+    )
+
+
+def _warning(code: str, message: str) -> SourceCandidateOperatorExternalReviewFinding:
+    return SourceCandidateOperatorExternalReviewFinding(
+        severity="warning",
+        code=code,
+        message=message,
+    )
+
+
+def _atomic_write_json(path: Path, payload: dict[str, object]) -> None:
+    path.parent.mkdir(parents=True, exist_ok=True)
+    text = json.dumps(payload, ensure_ascii=False, indent=2, sort_keys=True) + "\n"
+    with tempfile.NamedTemporaryFile(
+        "w",
+        encoding="utf-8",
+        dir=str(path.parent),
+        prefix=f".{path.name}.",
+        suffix=".tmp",
+        delete=False,
+    ) as handle:
+        tmp_name = handle.name
+        handle.write(text)
+    os.replace(tmp_name, path)
diff --git a/tests/context/test_source_candidate_operator_external_review_report.py b/tests/context/test_source_candidate_operator_external_review_report.py
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/tests/context/test_source_candidate_operator_external_review_report.py
@@ -0,0 +1,181 @@
+from __future__ import annotations
+
+import json
+from pathlib import Path
+
+from context.source_candidate_operator_external_review_report import (
+    build_source_candidate_operator_external_review_report,
+    read_source_candidate_operator_external_review_report,
+    render_source_candidate_operator_external_review_report,
+    write_source_candidate_operator_external_review_report,
+)
+
+
+def _write_bundle(path: Path, *, mutation_allowed: bool = False, include_adapter: bool = True) -> None:
+    path.mkdir(parents=True)
+
+    (path / "external_projection_contract.json").write_text(
+        json.dumps({"schema": "missipy.source_candidate.external_projection_contract.v1"}) + "\n",
+        encoding="utf-8",
+    )
+    (path / "github_projection_payload.json").write_text(
+        json.dumps({"schema": "missipy.source_candidate.github_projection_payload.v1"}) + "\n",
+        encoding="utf-8",
+    )
+    (path / "remote_mutation_gate.json").write_text(
+        json.dumps(
+            {
+                "schema": "missipy.source_candidate.remote_mutation_gate.v1",
+                "target_kind": "github_projection_payload",
+                "repository": "newicody/autodoc",
+                "mutation_allowed": mutation_allowed,
+                "operation_count": 1,
+                "issue_count": 0 if mutation_allowed else 1,
+                "issues": []
+                if mutation_allowed
+                else [
+                    {
+                        "severity": "error",
+                        "code": "operator_not_confirmed",
+                        "message": "operator confirmation is required",
+                    }
+                ],
+            }
+        )
+        + "\n",
+        encoding="utf-8",
+    )
+    if include_adapter:
+        (path / "github_adapter_dry_run.json").write_text(
+            json.dumps(
+                {
+                    "schema": "missipy.source_candidate.github_adapter_dry_run.v1",
+                    "plan": {
+                        "schema": "missipy.source_candidate.github_adapter_plan.v1",
+                        "repository": "newicody/autodoc",
+                        "project_key": None,
+                        "dry_run": True,
+                        "remote_mutation": False,
+                        "operation_count": 1,
+                        "operations": [],
+                    },
+                    "gate": {
+                        "schema": "missipy.source_candidate.remote_mutation_gate.v1",
+                        "mutation_allowed": mutation_allowed,
+                    },
+                }
+            )
+            + "\n",
+            encoding="utf-8",
+        )
+
+    artifacts = [
+        {
+            "role": "external_projection_contract",
+            "path": str(path / "external_projection_contract.json"),
+            "byte_count": 1,
+            "schema": "missipy.source_candidate.external_projection_contract.v1",
+        },
+        {
+            "role": "github_projection_payload",
+            "path": str(path / "github_projection_payload.json"),
+            "byte_count": 1,
+            "schema": "missipy.source_candidate.github_projection_payload.v1",
+        },
+        {
+            "role": "remote_mutation_gate",
+            "path": str(path / "remote_mutation_gate.json"),
+            "byte_count": 1,
+            "schema": "missipy.source_candidate.remote_mutation_gate.v1",
+        },
+    ]
+    if include_adapter:
+        artifacts.append(
+            {
+                "role": "github_adapter_dry_run",
+                "path": str(path / "github_adapter_dry_run.json"),
+                "byte_count": 1,
+                "schema": "missipy.source_candidate.github_adapter_dry_run.v1",
+            }
+        )
+
+    (path / "manifest.json").write_text(
+        json.dumps(
+            {
+                "schema": "missipy.source_candidate.github_export_bundle.v1",
+                "path": str(path),
+                "manifest_path": str(path / "manifest.json"),
+                "repository": "newicody/autodoc",
+                "mutation_allowed": mutation_allowed,
+                "dry_run": True,
+                "operation_count": 1,
+                "artifact_count": len(artifacts),
+                "byte_count": 4,
+                "artifacts": artifacts,
+            }
+        )
+        + "\n",
+        encoding="utf-8",
+    )
+
+
+def test_operator_external_review_report_blocks_on_gate_errors(tmp_path: Path) -> None:
+    bundle = tmp_path / "github_export_bundle"
+    _write_bundle(bundle, mutation_allowed=False)
+
+    report = build_source_candidate_operator_external_review_report(bundle)
+
+    assert report.repository == "newicody/autodoc"
+    assert report.dry_run is True
+    assert report.mutation_allowed is False
+    assert report.recommended_action == "fix_errors"
+    assert any(finding.code == "operator_not_confirmed" for finding in report.findings)
+
+
+def test_operator_external_review_report_ready_when_gate_passes(tmp_path: Path) -> None:
+    bundle = tmp_path / "github_export_bundle"
+    _write_bundle(bundle, mutation_allowed=True)
+
+    report = build_source_candidate_operator_external_review_report(bundle)
+
+    assert report.mutation_allowed is True
+    assert report.recommended_action == "operator_review"
+    assert report.finding_count == 0
+
+
+def test_operator_external_review_report_finds_missing_adapter_artifact(tmp_path: Path) -> None:
+    bundle = tmp_path / "github_export_bundle"
+    _write_bundle(bundle, mutation_allowed=True, include_adapter=False)
+
+    report = build_source_candidate_operator_external_review_report(bundle)
+
+    assert report.recommended_action == "fix_errors"
+    assert any(finding.code == "missing_artifact" for finding in report.findings)
+    assert any(finding.code == "adapter_missing_or_unreadable" for finding in report.findings)
+
+
+def test_operator_external_review_report_writes_and_reads_json(tmp_path: Path) -> None:
+    bundle = tmp_path / "github_export_bundle"
+    output = tmp_path / "operator_external_review_report.json"
+    _write_bundle(bundle, mutation_allowed=True)
+    report = build_source_candidate_operator_external_review_report(bundle)
+
+    returned = write_source_candidate_operator_external_review_report(output, report)
+    payload = read_source_candidate_operator_external_review_report(output)
+
+    assert returned == output
+    assert payload["schema"] == "missipy.source_candidate.operator_external_review_report.v1"
+    assert payload["recommended_action"] == "operator_review"
+
+
+def test_operator_external_review_report_render_is_stable(tmp_path: Path) -> None:
+    bundle = tmp_path / "github_export_bundle"
+    _write_bundle(bundle, mutation_allowed=False)
+
+    text = render_source_candidate_operator_external_review_report(
+        build_source_candidate_operator_external_review_report(bundle)
+    )
+
+    assert "operator external review: BLOCKED" in text
+    assert "repository: newicody/autodoc" in text
+    assert "operator_not_confirmed" in text
diff --git a/tests/rules/test_source_candidate_operator_external_review_report_rule.py b/tests/rules/test_source_candidate_operator_external_review_report_rule.py
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/tests/rules/test_source_candidate_operator_external_review_report_rule.py
@@ -0,0 +1,37 @@
+from __future__ import annotations
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+
+
+def _read(path: str) -> str:
+    return (ROOT / path).read_text(encoding="utf-8")
+
+
+def test_operator_external_review_report_is_local_only() -> None:
+    source = _read("src/context/source_candidate_operator_external_review_report.py").lower()
+    forbidden = ("requests", "urllib", "httpx", "subprocess", "token", "authorization")
+    for token in forbidden:
+        assert token not in source
+
+
+def test_operator_external_review_report_reads_bundle_artifacts() -> None:
+    source = _read("src/context/source_candidate_operator_external_review_report.py")
+    assert "manifest.json" in source
+    assert "remote_mutation_gate" in source
+    assert "github_adapter_dry_run" in source
+
+
+def test_operator_external_review_report_uses_doc_manifest_path() -> None:
+    manifest = _read("doc/manifests/MANIFEST_PHASE7_8_CHANGED_FILES.md")
+    assert "src/kernel/scheduler.py" not in manifest
+    assert "src/context/source_candidate_operator_external_review_report.py" in manifest
+
+
+def test_operator_external_review_report_has_architecture_source_only() -> None:
+    dot = ROOT / "doc/docs/architecture/context/64_source_candidate_operator_external_review_report.dot"
+    svg = ROOT / "doc/docs/architecture/context/64_source_candidate_operator_external_review_report.svg"
+    assert dot.exists()
+    assert not svg.exists()
diff --git a/doc/docs/architecture/context/64_source_candidate_operator_external_review_report.dot b/doc/docs/architecture/context/64_source_candidate_operator_external_review_report.dot
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/docs/architecture/context/64_source_candidate_operator_external_review_report.dot
@@ -0,0 +1,17 @@
+digraph source_candidate_operator_external_review_report {
+  graph [rankdir=LR, label="Phase 7.8 SourceCandidate operator external review report", labelloc=t];
+  node [shape=box];
+
+  bundle [label="github_export_bundle/manifest.json"];
+  gate [label="remote_mutation_gate.json"];
+  adapter [label="github_adapter_dry_run.json\nfake-only"];
+  report [label="operator_external_review_report.json"];
+  operator [label="operator review decision"];
+  api [label="external API\nnot contacted", style=dashed];
+
+  bundle -> report;
+  gate -> report;
+  adapter -> report;
+  report -> operator;
+  report -> api [label="no call", style=dashed];
+}
diff --git a/doc/releases/README_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md b/doc/releases/README_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/releases/README_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md
@@ -0,0 +1,17 @@
+# Phase 7.8 — Operator External Review Report
+
+Phase 7.8 adds an operator-facing external review report.
+
+The report reads a local GitHub export bundle and summarizes:
+
+```text
+repository
+dry_run status
+remote mutation gate status
+operation count
+bundle artifacts
+findings
+recommended operator action
+```
+
+It is local-only and does not contact any external service.
diff --git a/doc/changelogs/CHANGELOG_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md b/doc/changelogs/CHANGELOG_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/changelogs/CHANGELOG_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md
@@ -0,0 +1,26 @@
+# Changelog — Phase 7.8 Operator External Review Report
+
+## Added
+
+- `src/context/source_candidate_operator_external_review_report.py`
+  - Reads a local GitHub export bundle.
+  - Inspects bundle manifest, remote mutation gate and fake adapter dry-run artifacts.
+  - Produces JSON and text reports for operator review.
+
+- `tests/context/test_source_candidate_operator_external_review_report.py`
+  - Covers blocked gate findings, passing review state, missing artifacts, JSON IO and text rendering.
+
+- `tests/rules/test_source_candidate_operator_external_review_report_rule.py`
+  - Ensures the report remains local-only and uses the doc manifest layout.
+
+- `doc/docs/architecture/context/64_source_candidate_operator_external_review_report.dot`
+  - Documents the operator external review boundary.
+
+## Not added
+
+- No real GitHub adapter.
+- No GitHub API calls.
+- No network.
+- No token handling.
+- No remote mutation.
+- No Scheduler change.
diff --git a/doc/code-rules/PHASE7_8_CODE_RULE_ALIGNMENT.md b/doc/code-rules/PHASE7_8_CODE_RULE_ALIGNMENT.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/code-rules/PHASE7_8_CODE_RULE_ALIGNMENT.md
@@ -0,0 +1,20 @@
+# Phase 7.8 Code Rule Alignment
+
+```text
+code_rule_review: done
+code_rule_update_required: false
+code_rule_reason: Phase 7.8 adds a local operator external review report built from the local GitHub export bundle, remote mutation gate and fake adapter dry-run artifacts. It does not introduce network access, token handling, real remote mutation, a scheduler path or an external backend.
+
+live_path_status: n/a
+live_path_uses_real_backend: false
+external_dependencies_added: false
+scheduler_modified: false
+network_added: false
+github_api_added: false
+qdrant_added: false
+llm_or_openvino_added: false
+search_commands_bounded: n/a
+```
+
+The phase gives the operator a stable local review artifact before any external
+read-only probe or real adapter work.
diff --git a/doc/reports/phase7/PHASE7_8_TEST_REPORT.md b/doc/reports/phase7/PHASE7_8_TEST_REPORT.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/reports/phase7/PHASE7_8_TEST_REPORT.md
@@ -0,0 +1,36 @@
+# Phase 7.8 Test Report
+
+## Commands
+
+```bash
+PYTHONPATH=src:. python -m compileall -q src tests tools
+PYTHONPATH=src:. pytest -q tests/context/test_source_candidate_operator_external_review_report.py
+PYTHONPATH=src:. pytest -q tests/rules
+PYTHONPATH=src:. pytest -q
+```
+
+## Expected
+
+```text
+operator external review report tests: pass
+rules: pass
+full suite: pass
+```
+
+## Code rule block
+
+```text
+code_rule_review: done
+code_rule_update_required: false
+code_rule_reason: Local operator external review report only; no network, no token handling, no real remote mutation.
+
+live_path_status: n/a
+live_path_uses_real_backend: false
+external_dependencies_added: false
+scheduler_modified: false
+network_added: false
+github_api_added: false
+qdrant_added: false
+llm_or_openvino_added: false
+search_commands_bounded: n/a
+```
diff --git a/doc/manifests/MANIFEST_PHASE7_8_CHANGED_FILES.md b/doc/manifests/MANIFEST_PHASE7_8_CHANGED_FILES.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/manifests/MANIFEST_PHASE7_8_CHANGED_FILES.md
@@ -0,0 +1,21 @@
+# Manifest — Phase 7.8 Operator External Review Report
+
+## Added
+
+- `src/context/source_candidate_operator_external_review_report.py`
+- `tests/context/test_source_candidate_operator_external_review_report.py`
+- `tests/rules/test_source_candidate_operator_external_review_report_rule.py`
+- `doc/docs/architecture/context/64_source_candidate_operator_external_review_report.dot`
+- `doc/releases/README_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md`
+- `doc/changelogs/CHANGELOG_PHASE7_8_OPERATOR_EXTERNAL_REVIEW_REPORT.md`
+- `doc/code-rules/PHASE7_8_CODE_RULE_ALIGNMENT.md`
+- `doc/reports/phase7/PHASE7_8_TEST_REPORT.md`
+- `doc/manifests/MANIFEST_PHASE7_8_CHANGED_FILES.md`
+
+## Modified
+
+- None.
+
+## Generated artifacts
+
+- No SVG is versioned.
