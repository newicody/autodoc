# 0112-route_bridge_boundary

Adds a bounded, no-I/O route bridge boundary for future NetworkBridge/HardwareBridge work.

Apply:

```bash
python apply_patch_queue.py --patch 0112-route_bridge_boundary --dry-run
python apply_patch_queue.py --patch 0112-route_bridge_boundary --commit --push
```
