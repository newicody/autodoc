diff --git a/PHASE0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_REPORT.md b/PHASE0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_REPORT.md
new file mode 100644
index 0000000..045bdcd
--- /dev/null
+++ b/PHASE0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_REPORT.md
@@ -0,0 +1,26 @@
+# Phase 0286-r4 — Specialist capability-growth ProjectV2 fields and views
+
+## Result
+
+The versioned `newicody/projects` bundle now declares the nine review fields
+prepared by the immutable r2 projection and one table view named
+`Révisions spécialistes`.
+
+The configuration remains declarative. GitHub Projects is a review and
+workflow surface, SQL remains the durable authority, and the existing
+Scheduler remains the only orchestration authority. Qdrant remains limited to
+projection and recall.
+
+## Scope
+
+- extend `projectv2_views.json` without deleting existing fields or views;
+- preserve machine values used by the local r2 review projection;
+- reuse the existing preview-first ProjectV2 reconciler;
+- update the cumulative `INSTALLATION.md` to `0286-r4`;
+- leave workflows, permissions, variables and secrets unchanged.
+
+## Verification
+
+The phase checks JSON validity, field uniqueness, exact option values, the
+specialist review view, cumulative installation markers and audit progression
+to `0286-r5-specialist-capability-growth-projects-publication-plan`.
diff --git a/doc/CHANGELOG_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md b/doc/CHANGELOG_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md
new file mode 100644
index 0000000..c2e2a25
--- /dev/null
+++ b/doc/CHANGELOG_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md
@@ -0,0 +1,13 @@
+# Changelog — 0286-r4
+
+- Added nine specialist capability-growth review fields to the versioned
+  `newicody/projects` configuration.
+- Added the table view `Révisions spécialistes`.
+- Added an explicit projection-to-field mapping for the later publication
+  plan and adapter.
+- Updated cumulative `INSTALLATION.md` to version `0286-r4` with preview-first
+  reconciliation, exact digest confirmation and no `--delete`.
+- Preserved Scheduler, SQL, Qdrant, EventBus and GitHub Projects authority
+  boundaries.
+- Set the next patch to
+  `0286-r5-specialist-capability-growth-projects-publication-plan`.
diff --git a/doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.dot b/doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.dot
new file mode 100644
index 0000000..d8c76b9
--- /dev/null
+++ b/doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.dot
@@ -0,0 +1,14 @@
+digraph specialist_capability_growth_projectv2_fields_views_0286 {
+  rankdir=LR;
+  node [shape=box];
+
+  local [label="0285 closed local evidence\nSQL authoritative"];
+  projection [label="0286-r2 immutable review projection"];
+  fields [label="0286-r4 versioned fields"];
+  view [label="Révisions spécialistes\nreview table"];
+  reconciler [label="existing reconciler\npreview + digest + locks"];
+  github [label="GitHub Projects\nnon-authoritative"];
+
+  local -> projection -> fields -> view;
+  fields -> reconciler -> github [style=dashed, label="operator-authorized later"];
+}
diff --git a/doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.md b/doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.md
new file mode 100644
index 0000000..7908fa5
--- /dev/null
+++ b/doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.md
@@ -0,0 +1,36 @@
+# Specialist capability-growth ProjectV2 fields and views — 0286-r4
+
+## Decision
+
+Reuse the existing versioned Projects configuration and reconciler. Do not add
+a second ProjectV2 client or a specialist registry.
+
+The nine fields mirror the immutable r2 projection exactly. Text fields carry
+typed references and digests. Single-select fields carry stable machine values
+so later publication and readback phases can compare exact values without
+translation drift.
+
+## Authority boundary
+
+```text
+local proposal/revision/operator gate
+  -> SQL durable history
+  -> Scheduler selection
+  -> immutable r2 review projection
+  -> r4 ProjectV2 field declaration
+  -> later operator-authorized publication
+
+GitHub Projects = review/workflow surface
+SQL             = durable authority
+Scheduler       = orchestration authority
+Qdrant          = projection/recall only
+```
+
+The r4 configuration performs no mutation by itself. Installation uses the
+existing preview, exact plan digest and two explicit environment locks.
+
+## Next phase
+
+`0286-r5-specialist-capability-growth-projects-publication-plan` composes the
+Issue comment and ProjectV2 field values into one deterministic, non-executing
+publication plan.
diff --git a/doc/manifests/MANIFEST_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md b/doc/manifests/MANIFEST_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md
new file mode 100644
index 0000000..87aff5d
--- /dev/null
+++ b/doc/manifests/MANIFEST_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md
@@ -0,0 +1,25 @@
+# Manifest — 0286-r4 specialist capability-growth ProjectV2 fields/views
+
+## Modified
+
+- `templates/github/projects-repository/projectv2_views.json`
+- `templates/github/projects-repository/INSTALLATION.md`
+
+## Added
+
+- `tests/context/test_specialist_capability_growth_projectv2_fields_views_0286.py`
+- `tests/rules/test_specialist_capability_growth_projectv2_fields_views_0286_rule.py`
+- `PHASE0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_REPORT.md`
+- `doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.md`
+- `doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.dot`
+- `doc/CHANGELOG_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md`
+- `doc/manifests/MANIFEST_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md`
+
+## Boundaries
+
+No workflow, permission, secret, variable, Scheduler, SQL adapter, Qdrant
+adapter, EventBus, laboratory backend, HTTP client or global specialist
+registry is added or modified. GitHub Projects remains non-authoritative.
+
+The cumulative `INSTALLATION.md` is reviewed and updated because the deployed
+Projects bundle changes in this phase.
diff --git a/templates/github/projects-repository/INSTALLATION.md b/templates/github/projects-repository/INSTALLATION.md
index 09d7def..312e69d 100644
--- a/templates/github/projects-repository/INSTALLATION.md
+++ b/templates/github/projects-repository/INSTALLATION.md
@@ -4,7 +4,7 @@ Ce document est le mode opératoire cumulatif du bundle GitHub fourni par
 Autodoc. Il doit être mis à jour chaque fois que la copie, les Actions, les
 variables, les secrets, les vues ou les contrats du dépôt Projects évoluent.
 
-Version du guide : `0286-r3`.
+Version du guide : `0286-r4`.
 
 ## Frontière
 
@@ -468,5 +468,93 @@ Ne pas utiliser `--delete` lors de cette mise à niveau.
 
 Repères historiques conservés pour les règles cumulatives :
 
+- Version du guide : `0286-r3`.
 - Version du guide : `0284-r9`.
 - Version du guide : `0284-r1-r5`.
+
+
+## 12. Installer les champs et la vue de revue des révisions spécialistes
+
+Le fichier `projectv2_views.json` déclare maintenant les champs suivants :
+
+```text
+Spécialiste
+Révision spécialiste
+Capacité proposée
+Action capacité
+Décision capacité
+Statut révision
+Référence SQL
+Digest décision
+Laboratoire
+```
+
+Les valeurs machine des champs à choix unique restent alignées sur les
+contrats locaux :
+
+```text
+Action capacité    = add | refine | deprecate | restore
+Décision capacité  = pending | approve | reject | defer
+Statut révision    = request_received | proposal_ready |
+                     approved_selected_observed | published |
+                     readback_verified | rejected | deferred
+```
+
+Ces champs sont une projection de revue. Ils ne remplacent ni la décision
+opérateur locale, ni l'historique SQL durable, ni la sélection réalisée par le
+Scheduler existant.
+
+Depuis la copie locale de `newicody/projects`, préparer le plan sans mutation :
+
+```bash
+cd /home/eric/projet/git/projects
+export AUTODOC_PROJECT_TOKEN='...'
+
+python scripts/reconcile_projectv2_configuration.py   --config projectv2_views.json   --format json
+```
+
+Contrôler notamment :
+
+```text
+valid=true
+missing_fields
+missing_views
+field_option_drift
+unresolved_visible_fields
+manual_layout_steps
+plan_digest
+```
+
+Le plan doit proposer la vue table `Révisions spécialistes`. Il ne doit
+supprimer ni champ ni vue existants. Si un champ single-select existe déjà mais
+ne contient pas toutes les options, le réconciliateur signale une dérive sans
+la corriger automatiquement.
+
+Après validation explicite du plan, ouvrir les deux verrous de configuration
+et confirmer le digest exact :
+
+```bash
+export AUTODOC_REMOTE_MUTATION_ALLOWED=true
+export AUTODOC_PROJECT_CONFIGURATION_ALLOWED=true
+
+python scripts/reconcile_projectv2_configuration.py   --config projectv2_views.json   --execute   --confirm-plan-digest '<PLAN_DIGEST>'   --format json
+```
+
+Dans l'interface GitHub, vérifier ensuite la présence de la vue
+`Révisions spécialistes` et l'ordre de ses colonnes. Aucun nouveau secret,
+aucune permission Actions et aucune modification du workflow ne sont requis
+par `0286-r4`.
+
+Comparer enfin le bundle source et la copie déployée :
+
+```bash
+rsync -aivn   --exclude README.md   "$SRC"/   "$DST"/
+```
+
+Ne pas utiliser `--delete` lors de cette mise à niveau.
+
+## Historique du guide — champs de revue des spécialistes
+
+| Phase | Évolution |
+|---|---|
+| `0286-r4` | Ajout des neuf champs de projection et de la vue `Révisions spécialistes`, avec installation preview-first et confirmation par digest. |
diff --git a/templates/github/projects-repository/projectv2_views.json b/templates/github/projects-repository/projectv2_views.json
index 9dae1f9..2e32a1a 100644
--- a/templates/github/projects-repository/projectv2_views.json
+++ b/templates/github/projects-repository/projectv2_views.json
@@ -64,7 +64,46 @@
     {"name": "Confiance Copilot", "data_type": "number"},
     {"name": "Dernière mise à jour", "data_type": "date"},
     {"name": "Artefact", "data_type": "text"},
-    {"name": "Cycle", "data_type": "text"}
+    {"name": "Cycle", "data_type": "text"},
+    {"name": "Spécialiste", "data_type": "text"},
+    {"name": "Révision spécialiste", "data_type": "text"},
+    {"name": "Capacité proposée", "data_type": "text"},
+    {
+      "name": "Action capacité",
+      "data_type": "single_select",
+      "options": [
+        {"name": "add", "color": "GREEN", "description": "Ajouter une capacité"},
+        {"name": "refine", "color": "BLUE", "description": "Affiner une capacité existante"},
+        {"name": "deprecate", "color": "RED", "description": "Déprécier une capacité"},
+        {"name": "restore", "color": "PURPLE", "description": "Restaurer une capacité dépréciée"}
+      ]
+    },
+    {
+      "name": "Décision capacité",
+      "data_type": "single_select",
+      "options": [
+        {"name": "pending", "color": "GRAY", "description": "Décision opérateur non rendue"},
+        {"name": "approve", "color": "GREEN", "description": "Révision autorisée par l'opérateur"},
+        {"name": "reject", "color": "RED", "description": "Révision refusée par l'opérateur"},
+        {"name": "defer", "color": "YELLOW", "description": "Décision opérateur différée"}
+      ]
+    },
+    {
+      "name": "Statut révision",
+      "data_type": "single_select",
+      "options": [
+        {"name": "request_received", "color": "GRAY", "description": "Demande reçue, sans proposition autoritative"},
+        {"name": "proposal_ready", "color": "BLUE", "description": "Proposition locale prête pour revue"},
+        {"name": "approved_selected_observed", "color": "GREEN", "description": "Révision approuvée, sélectionnée et observée"},
+        {"name": "published", "color": "PURPLE", "description": "Projection GitHub publiée sous autorisation"},
+        {"name": "readback_verified", "color": "GREEN", "description": "Relecture query-only vérifiée"},
+        {"name": "rejected", "color": "RED", "description": "Révision refusée"},
+        {"name": "deferred", "color": "YELLOW", "description": "Révision différée"}
+      ]
+    },
+    {"name": "Référence SQL", "data_type": "text"},
+    {"name": "Digest décision", "data_type": "text"},
+    {"name": "Laboratoire", "data_type": "text"}
   ],
   "copilot_projection": {
     "status_field": "Copilot",
@@ -76,6 +115,20 @@
     "artifact_field": "Artefact",
     "cycle_field": "Cycle"
   },
+  "specialist_capability_growth_projection": {
+    "specialist_field": "Spécialiste",
+    "revision_field": "Révision spécialiste",
+    "capability_field": "Capacité proposée",
+    "action_field": "Action capacité",
+    "decision_field": "Décision capacité",
+    "revision_status_field": "Statut révision",
+    "sql_ref_field": "Référence SQL",
+    "decision_digest_field": "Digest décision",
+    "laboratory_field": "Laboratoire",
+    "allowed_actions": ["add", "refine", "deprecate", "restore"],
+    "allowed_decisions": ["pending", "approve", "reject", "defer"],
+    "review_status_value": "approved_selected_observed"
+  },
   "views": [
     {
       "name": "Recherches",
@@ -115,6 +168,12 @@
       "filter": "Affichage:Historique",
       "visible_fields": ["Title", "Status", "Thème", "Type", "Résumé", "Copilot", "Dernière mise à jour", "Cycle"]
     },
+    {
+      "name": "Révisions spécialistes",
+      "layout": "table",
+      "filter": "Action capacité:add,refine,deprecate,restore",
+      "visible_fields": ["Title", "Status", "Spécialiste", "Révision spécialiste", "Capacité proposée", "Action capacité", "Décision capacité", "Statut révision", "Référence SQL", "Digest décision", "Laboratoire", "Dernière mise à jour"]
+    },
     {
       "name": "Tous",
       "layout": "table",
diff --git a/tests/context/test_specialist_capability_growth_projectv2_fields_views_0286.py b/tests/context/test_specialist_capability_growth_projectv2_fields_views_0286.py
new file mode 100644
index 0000000..47c0664
--- /dev/null
+++ b/tests/context/test_specialist_capability_growth_projectv2_fields_views_0286.py
@@ -0,0 +1,139 @@
+from __future__ import annotations
+
+import json
+from pathlib import Path
+
+from context.specialist_capability_growth_projects_operator_workflow_reuse_audit_0286 import (
+    DEVELOPMENT_PHASES,
+    REQUIRED_REUSE_SURFACES,
+    audit_specialist_capability_growth_projects_operator_workflow_reuse,
+)
+
+ROOT = Path(__file__).resolve().parents[2]
+CONFIG = ROOT / "templates/github/projects-repository/projectv2_views.json"
+INSTALLATION = ROOT / "templates/github/projects-repository/INSTALLATION.md"
+
+EXPECTED_FIELDS = (
+    "Spécialiste",
+    "Révision spécialiste",
+    "Capacité proposée",
+    "Action capacité",
+    "Décision capacité",
+    "Statut révision",
+    "Référence SQL",
+    "Digest décision",
+    "Laboratoire",
+)
+
+
+def _configuration() -> dict[str, object]:
+    return json.loads(CONFIG.read_text(encoding="utf-8"))
+
+
+def _field_map() -> dict[str, dict[str, object]]:
+    configuration = _configuration()
+    fields = configuration["fields"]
+    assert isinstance(fields, list)
+    return {str(item["name"]): item for item in fields}
+
+
+def test_specialist_review_fields_are_declared_once() -> None:
+    fields = _field_map()
+    assert all(name in fields for name in EXPECTED_FIELDS)
+    assert len(fields) == len(set(fields))
+    assert fields["Spécialiste"]["data_type"] == "text"
+    assert fields["Révision spécialiste"]["data_type"] == "text"
+    assert fields["Référence SQL"]["data_type"] == "text"
+    assert fields["Digest décision"]["data_type"] == "text"
+
+
+def test_machine_values_match_the_local_projection_contract() -> None:
+    fields = _field_map()
+
+    def option_names(name: str) -> tuple[str, ...]:
+        options = fields[name]["options"]
+        assert isinstance(options, list)
+        return tuple(str(option["name"]) for option in options)
+
+    assert option_names("Action capacité") == (
+        "add",
+        "refine",
+        "deprecate",
+        "restore",
+    )
+    assert option_names("Décision capacité") == (
+        "pending",
+        "approve",
+        "reject",
+        "defer",
+    )
+    assert "approved_selected_observed" in option_names("Statut révision")
+    assert "readback_verified" in option_names("Statut révision")
+
+
+def test_projection_mapping_reuses_exact_field_names() -> None:
+    configuration = _configuration()
+    projection = configuration["specialist_capability_growth_projection"]
+    assert isinstance(projection, dict)
+    projected_fields = tuple(
+        projection[key]
+        for key in (
+            "specialist_field",
+            "revision_field",
+            "capability_field",
+            "action_field",
+            "decision_field",
+            "revision_status_field",
+            "sql_ref_field",
+            "decision_digest_field",
+            "laboratory_field",
+        )
+    )
+    assert projected_fields == EXPECTED_FIELDS
+    assert projection["review_status_value"] == "approved_selected_observed"
+
+
+def test_specialist_revision_review_view_is_table_only() -> None:
+    configuration = _configuration()
+    views = configuration["views"]
+    assert isinstance(views, list)
+    matching = [view for view in views if view.get("name") == "Révisions spécialistes"]
+    assert len(matching) == 1
+    view = matching[0]
+    assert view["layout"] == "table"
+    assert view["filter"] == "Action capacité:add,refine,deprecate,restore"
+    for name in EXPECTED_FIELDS:
+        assert name in view["visible_fields"]
+    assert "manual_layout" not in view
+
+
+def test_audit_advances_through_r4_to_publication_plan() -> None:
+    sources: dict[str, str] = {
+        requirement.path: "\n".join(requirement.markers)
+        for requirement in REQUIRED_REUSE_SURFACES
+    }
+    workflow_path = (
+        "templates/github/projects-repository/.github/workflows/"
+        "autodoc-controlled-research.yml"
+    )
+    sources[workflow_path] += "\nissues: read\nactions: read\n"
+
+    for phase in DEVELOPMENT_PHASES[:3]:
+        for requirement in phase.requirements:
+            if requirement.path.endswith("projectv2_views.json"):
+                sources[requirement.path] = CONFIG.read_text(encoding="utf-8")
+            elif requirement.path.endswith("INSTALLATION.md"):
+                sources[requirement.path] = INSTALLATION.read_text(encoding="utf-8")
+            else:
+                sources[requirement.path] = "\n".join(requirement.markers)
+
+    result = audit_specialist_capability_growth_projects_operator_workflow_reuse(
+        sources
+    )
+    assert result.valid is True
+    assert result.completed_phases[:3] == tuple(
+        phase.patch_id for phase in DEVELOPMENT_PHASES[:3]
+    )
+    assert result.next_recommended_patch == DEVELOPMENT_PHASES[3].patch_id
+    assert result.specialist_revision_fields_present is True
+    assert result.operator_decision_view_present is True
diff --git a/tests/rules/test_specialist_capability_growth_projectv2_fields_views_0286_rule.py b/tests/rules/test_specialist_capability_growth_projectv2_fields_views_0286_rule.py
new file mode 100644
index 0000000..f9fafc9
--- /dev/null
+++ b/tests/rules/test_specialist_capability_growth_projectv2_fields_views_0286_rule.py
@@ -0,0 +1,71 @@
+from __future__ import annotations
+
+import json
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[2]
+CONFIG = ROOT / "templates/github/projects-repository/projectv2_views.json"
+INSTALLATION = ROOT / "templates/github/projects-repository/INSTALLATION.md"
+REPORT = ROOT / "PHASE0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_REPORT.md"
+ARCHITECTURE = ROOT / "doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.md"
+GRAPH = ROOT / "doc/architecture/SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS_0286.dot"
+CHANGELOG = ROOT / "doc/CHANGELOG_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md"
+MANIFEST = ROOT / "doc/manifests/MANIFEST_0286_R4_SPECIALIST_CAPABILITY_GROWTH_PROJECTV2_FIELDS_VIEWS.md"
+
+
+def test_projectv2_configuration_keeps_one_schema_and_user_project_identity() -> None:
+    configuration = json.loads(CONFIG.read_text(encoding="utf-8"))
+    assert configuration["schema"] == (
+        "autodoc.github.projects_repository_configuration.v1"
+    )
+    assert configuration["project"] == {
+        "owner_kind": "user",
+        "owner": "newicody",
+        "number": 3,
+    }
+
+
+def test_review_surface_contains_no_authority_or_execution_claim() -> None:
+    text = CONFIG.read_text(encoding="utf-8")
+    forbidden = (
+        "github_projects_authoritative",
+        "sql_write_allowed",
+        "scheduler_dispatch_allowed",
+        "laboratory_execution_allowed",
+        "AUTODOC_REMOTE_MUTATION_ALLOWED=true",
+        "gh api",
+    )
+    for marker in forbidden:
+        assert marker not in text
+
+
+def test_installation_is_cumulative_and_preview_first() -> None:
+    text = INSTALLATION.read_text(encoding="utf-8")
+    assert "Version du guide : `0286-r4`." in text
+    assert "Version du guide : `0286-r3`." in text
+    assert "Version du guide : `0284-r9`." in text
+    assert "Version du guide : `0284-r1-r5`." in text
+    assert "Révisions spécialistes" in text
+    assert "--confirm-plan-digest '<PLAN_DIGEST>'" in text
+    assert "Ne pas utiliser `--delete`" in text
+    assert "AUTODOC_COPILOT_ADVISORY_ENABLED=false" in text
+
+
+def test_phase_deliverables_exist_and_lock_existing_boundaries() -> None:
+    for path in (REPORT, ARCHITECTURE, GRAPH, CHANGELOG, MANIFEST):
+        assert path.is_file(), path
+    combined = "\n".join(
+        path.read_text(encoding="utf-8")
+        for path in (REPORT, ARCHITECTURE, CHANGELOG, MANIFEST)
+    )
+    for marker in (
+        "Scheduler",
+        "SQL",
+        "Qdrant",
+        "GitHub Projects",
+        "INSTALLATION.md",
+        "0286-r5-specialist-capability-growth-projects-publication-plan",
+    ):
+        assert marker in combined
+    assert "new Scheduler" not in combined
+    assert "LaboratoryManager" not in combined
