# 0165 — Project push frame and fcron ConfigObj

Apply:

```text
tar -xJf autodoc_patch_0165-project_push_frame_fcron_config.tar.xz
python apply_patch_queue.py --patch 0165-project_push_frame_fcron_config --dry-run --allow-dirty
python apply_patch_queue.py --patch 0165-project_push_frame_fcron_config --commit --push --allow-dirty
```
