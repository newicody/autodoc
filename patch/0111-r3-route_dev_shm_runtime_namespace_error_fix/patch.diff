diff --git a/src/runtime/route_dev_shm_runtime.py b/src/runtime/route_dev_shm_runtime.py
--- a/src/runtime/route_dev_shm_runtime.py
+++ b/src/runtime/route_dev_shm_runtime.py
@@ -203,7 +203,7 @@ def _ensure_relative_to_root(*, root: Path, child: Path) -> None:
     try:
         child.resolve(strict=False).relative_to(root.resolve(strict=True))
     except ValueError as exc:
-        raise RouteDevShmRuntimeError("runtime root must remain under dev_shm_root") from exc
+        raise RouteDevShmRuntimeError("namespace root must remain under dev_shm_root") from exc
 
 
 def _reject_symlink(path: Path, *, label: str) -> None:
