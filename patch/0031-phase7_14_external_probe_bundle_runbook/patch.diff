diff --git a/doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md b/doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md
@@ -0,0 +1,126 @@
+# SourceCandidate External Probe Bundle Runbook
+
+This runbook describes the local operator workflow for the SourceCandidate
+external probe path.
+
+The workflow remains local-only:
+
+```text
+no external service call
+no token handling
+no remote mutation
+no Scheduler execution
+```
+
+## Inputs
+
+The operator starts from local artifacts generated by the previous phases:
+
+```text
+operator_external_review_report.json
+read_only_external_probe_request.json
+read_only_external_probe_result.json
+```
+
+The expected schemas are:
+
+```text
+missipy.source_candidate.operator_external_review_report.v1
+missipy.source_candidate.read_only_external_probe_request.v1
+missipy.source_candidate.read_only_external_probe_result.v1
+```
+
+## Dry-run plan
+
+Use the CLI without `--apply` first:
+
+```bash
+python tools/source_candidate_external_probe_bundle_cli.py \
+  --output-dir /tmp/source_candidate_external_probe_bundle \
+  --operator-review-report /path/to/operator_external_review_report.json \
+  --probe-request /path/to/read_only_external_probe_request.json \
+  --probe-result /path/to/read_only_external_probe_result.json
+```
+
+Expected result:
+
+```text
+external probe bundle cli: dry-run
+writes_bundle: False
+```
+
+## Apply local bundle
+
+Only after the dry-run plan is correct:
+
+```bash
+python tools/source_candidate_external_probe_bundle_cli.py \
+  --output-dir /tmp/source_candidate_external_probe_bundle \
+  --operator-review-report /path/to/operator_external_review_report.json \
+  --probe-request /path/to/read_only_external_probe_request.json \
+  --probe-result /path/to/read_only_external_probe_result.json \
+  --apply
+```
+
+Expected bundle:
+
+```text
+/tmp/source_candidate_external_probe_bundle/
+  manifest.json
+  operator_external_review_report.json
+  read_only_external_probe_request.json
+  read_only_external_probe_result.json
+```
+
+## Validate repository state
+
+Run the normal local gates:
+
+```bash
+PYTHONPATH=src:. python -m compileall -q src tests tools
+PYTHONPATH=src:. pytest -q tests/rules
+PYTHONPATH=src:. pytest -q
+```
+
+## Optional documentation SVG workflow
+
+If documentation SVG files were rebuilt, apply the SVG build policy before rule
+tests:
+
+```bash
+cd doc
+make
+cd ..
+
+python tools/docs_svg_build_policy.py \
+  --root . \
+  --clean \
+  --check \
+  --report-file doc/maintenance/docs_svg_build_policy_report.json
+```
+
+## Operator checklist
+
+Before using the bundle as a handoff artifact, verify:
+
+```text
+[ ] dry-run plan was reviewed
+[ ] bundle was created only with --apply
+[ ] manifest.json exists
+[ ] read_only is true
+[ ] external_call_performed is false
+[ ] probe_allowed has been reviewed
+[ ] pytest rules pass
+[ ] full pytest suite passes
+```
+
+## Failure handling
+
+If the CLI reports a schema mismatch, regenerate the input artifact that failed
+validation.
+
+If the CLI reports a repository mismatch, stop and inspect the three input JSON
+files. A bundle must never merge artifacts from different repositories.
+
+If rule tests fail because generated SVG files exist under
+`doc/docs/architecture/context`, run the documentation SVG build policy cleanup.
diff --git a/tests/docs/test_source_candidate_external_probe_bundle_runbook.py b/tests/docs/test_source_candidate_external_probe_bundle_runbook.py
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/tests/docs/test_source_candidate_external_probe_bundle_runbook.py
@@ -0,0 +1,49 @@
+from __future__ import annotations
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+RUNBOOK = ROOT / "doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md"
+
+
+def test_external_probe_bundle_runbook_exists() -> None:
+    assert RUNBOOK.exists()
+
+
+def test_external_probe_bundle_runbook_documents_dry_run_before_apply() -> None:
+    text = RUNBOOK.read_text(encoding="utf-8")
+
+    dry_run_index = text.index("## Dry-run plan")
+    apply_index = text.index("## Apply local bundle")
+
+    assert dry_run_index < apply_index
+    assert "without `--apply` first" in text
+    assert "writes_bundle: False" in text
+
+
+def test_external_probe_bundle_runbook_documents_expected_bundle_files() -> None:
+    text = RUNBOOK.read_text(encoding="utf-8")
+
+    assert "manifest.json" in text
+    assert "operator_external_review_report.json" in text
+    assert "read_only_external_probe_request.json" in text
+    assert "read_only_external_probe_result.json" in text
+
+
+def test_external_probe_bundle_runbook_documents_safety_flags() -> None:
+    text = RUNBOOK.read_text(encoding="utf-8")
+
+    assert "no external service call" in text
+    assert "no token handling" in text
+    assert "no remote mutation" in text
+    assert "no Scheduler execution" in text
+    assert "external_call_performed is false" in text
+
+
+def test_external_probe_bundle_runbook_references_svg_policy_after_make() -> None:
+    text = RUNBOOK.read_text(encoding="utf-8")
+
+    assert "tools/docs_svg_build_policy.py" in text
+    assert "--clean" in text
+    assert "--check" in text
diff --git a/tests/rules/test_source_candidate_external_probe_bundle_runbook_rule.py b/tests/rules/test_source_candidate_external_probe_bundle_runbook_rule.py
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/tests/rules/test_source_candidate_external_probe_bundle_runbook_rule.py
@@ -0,0 +1,37 @@
+from __future__ import annotations
+
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+RUNBOOK = ROOT / "doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md"
+
+
+def _read(path: str) -> str:
+    return (ROOT / path).read_text(encoding="utf-8")
+
+
+def test_external_probe_bundle_runbook_is_local_only() -> None:
+    text = RUNBOOK.read_text(encoding="utf-8").lower()
+    forbidden = ("curl ", "wget ", "requests", "httpx", "authorization", "personal access token")
+    for forbidden_text in forbidden:
+        assert forbidden_text not in text
+
+
+def test_external_probe_bundle_runbook_keeps_apply_explicit() -> None:
+    text = RUNBOOK.read_text(encoding="utf-8")
+    assert "Use the CLI without `--apply` first" in text
+    assert "Only after the dry-run plan is correct" in text
+    assert " --apply" in text
+
+
+def test_external_probe_bundle_runbook_manifest_uses_post_migration_path() -> None:
+    manifest = _read("doc/manifests/MANIFEST_PHASE7_14_CHANGED_FILES.md")
+    assert "src/kernel/scheduler.py" not in manifest
+    assert "doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md" in manifest
+
+
+def test_external_probe_bundle_runbook_does_not_add_dot_or_svg() -> None:
+    manifest = _read("doc/manifests/MANIFEST_PHASE7_14_CHANGED_FILES.md")
+    assert ".dot" not in manifest
+    assert ".svg" not in manifest
diff --git a/doc/releases/README_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md b/doc/releases/README_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/releases/README_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md
@@ -0,0 +1,16 @@
+# Phase 7.14 — External Probe Bundle Runbook
+
+Phase 7.14 adds an operator runbook for the local external probe bundle path.
+
+The runbook documents the safe sequence:
+
+```text
+operator review report
+read-only probe request
+read-only probe result
+CLI dry-run
+CLI apply
+local bundle validation
+```
+
+It does not add runtime behavior.
diff --git a/doc/changelogs/CHANGELOG_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md b/doc/changelogs/CHANGELOG_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/changelogs/CHANGELOG_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md
@@ -0,0 +1,24 @@
+# Changelog — Phase 7.14 External Probe Bundle Runbook
+
+## Added
+
+- `doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md`
+  - Documents dry-run before apply.
+  - Documents expected local bundle files.
+  - Documents safety flags and failure handling.
+  - References the documentation SVG build policy workflow.
+
+- `tests/docs/test_source_candidate_external_probe_bundle_runbook.py`
+  - Verifies the runbook structure and key operator safeguards.
+
+- `tests/rules/test_source_candidate_external_probe_bundle_runbook_rule.py`
+  - Ensures the runbook stays local-only and does not add DOT/SVG artifacts.
+
+## Not added
+
+- No source module.
+- No CLI change.
+- No DOT.
+- No SVG.
+- No network.
+- No Scheduler change.
diff --git a/doc/code-rules/PHASE7_14_CODE_RULE_ALIGNMENT.md b/doc/code-rules/PHASE7_14_CODE_RULE_ALIGNMENT.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/code-rules/PHASE7_14_CODE_RULE_ALIGNMENT.md
@@ -0,0 +1,20 @@
+# Phase 7.14 Code Rule Alignment
+
+```text
+code_rule_review: done
+code_rule_update_required: false
+code_rule_reason: Phase 7.14 adds an operator runbook for the local external probe bundle workflow. It is documentation-only and does not add runtime behavior, network access, scheduler execution, DOT graphs or SVG artifacts.
+
+live_path_status: n/a
+live_path_uses_real_backend: n/a
+external_dependencies_added: false
+scheduler_modified: false
+network_added: false
+github_api_added: false
+qdrant_added: false
+llm_or_openvino_added: false
+search_commands_bounded: n/a
+```
+
+The runbook gives the operator a safe repeatable sequence for local bundle
+generation and validation.
diff --git a/doc/reports/phase7/PHASE7_14_TEST_REPORT.md b/doc/reports/phase7/PHASE7_14_TEST_REPORT.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/reports/phase7/PHASE7_14_TEST_REPORT.md
@@ -0,0 +1,28 @@
+# Phase 7.14 Test Report
+
+## Commands
+
+```bash
+PYTHONPATH=src:. python -m compileall -q src tests tools
+PYTHONPATH=src:. pytest -q tests/docs/test_source_candidate_external_probe_bundle_runbook.py
+PYTHONPATH=src:. pytest -q tests/rules
+PYTHONPATH=src:. pytest -q
+```
+
+## Expected
+
+```text
+runbook tests: pass
+rules: pass
+full suite: pass
+```
+
+## Scope
+
+```text
+documentation-only
+local-only
+no DOT
+no SVG
+no Scheduler change
+```
diff --git a/doc/manifests/MANIFEST_PHASE7_14_CHANGED_FILES.md b/doc/manifests/MANIFEST_PHASE7_14_CHANGED_FILES.md
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/doc/manifests/MANIFEST_PHASE7_14_CHANGED_FILES.md
@@ -0,0 +1,20 @@
+# Manifest — Phase 7.14 External Probe Bundle Runbook
+
+## Added
+
+- `doc/runbooks/SOURCE_CANDIDATE_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md`
+- `tests/docs/test_source_candidate_external_probe_bundle_runbook.py`
+- `tests/rules/test_source_candidate_external_probe_bundle_runbook_rule.py`
+- `doc/releases/README_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md`
+- `doc/changelogs/CHANGELOG_PHASE7_14_EXTERNAL_PROBE_BUNDLE_RUNBOOK.md`
+- `doc/code-rules/PHASE7_14_CODE_RULE_ALIGNMENT.md`
+- `doc/reports/phase7/PHASE7_14_TEST_REPORT.md`
+- `doc/manifests/MANIFEST_PHASE7_14_CHANGED_FILES.md`
+
+## Modified
+
+- None.
+
+## Generated artifacts
+
+- None.
