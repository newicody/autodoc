# 0273-r3 — deterministic fake laboratory provider

Adds the stdlib-only fake provider and binds it with the existing 0257 registry type.

```bash
python apply_patch_queue.py \
  --patch 0273-r3-deterministic_fake_laboratory_provider \
  --dry-run \
  --allow-dirty
```
