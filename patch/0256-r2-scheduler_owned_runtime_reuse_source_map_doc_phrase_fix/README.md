# 0256-r2-scheduler_owned_runtime_reuse_source_map_doc_phrase_fix

Tiny documentation wording fix for the 0256 rule test.

Apply on top of 0256-r1 after the rule failure:

```bash
python apply_patch_queue.py --patch 0256-r2-scheduler_owned_runtime_reuse_source_map_doc_phrase_fix --commit --push --allow-dirty
```
