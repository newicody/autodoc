diff --git a/PHASE0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME_REPORT.md b/PHASE0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME_REPORT.md
new file mode 100644
index 0000000..be5cd53
--- /dev/null
+++ b/PHASE0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME_REPORT.md
@@ -0,0 +1,37 @@
+# Phase 0281-r4 report — pinned cached Copilot CLI runtime
+
+## Result
+
+The Autodoc templates now use:
+
+```text
+@github/copilot@1.0.70
+actions/cache@v4
+workflow-local complete npm prefix
+installation only on exact cache miss
+version verification
+COPILOT_AUTO_UPDATE=false
+```
+
+The deployed workflow in `newicody/projects` must receive the same change.
+
+## Phase review
+
+```text
+code_rule_review: done
+code_rule_update_required: false
+live_path_status: transition
+external_dependency_added: false
+external_dependency_existing: @github/copilot
+external_dependency_version: 1.0.70
+external_dependency_pinned: true
+scheduler_modified: false
+scheduler_run_modified: false
+github_mutation_added: false
+sql_write_added: false
+qdrant_write_added: false
+projects_repository_change_required: true
+projects_repository_change_reason: deploy workflow and allow actions/cache@v4 in newicody/projects
+```
+
+The next phase is `0281-r5-operator-laboratory-advisory-projection`.
diff --git a/doc/architecture/GITHUB_PINNED_CACHED_COPILOT_CLI_RUNTIME_0281.md b/doc/architecture/GITHUB_PINNED_CACHED_COPILOT_CLI_RUNTIME_0281.md
new file mode 100644
index 0000000..f240ece
--- /dev/null
+++ b/doc/architecture/GITHUB_PINNED_CACHED_COPILOT_CLI_RUNTIME_0281.md
@@ -0,0 +1,45 @@
+# GitHub pinned and cached Copilot CLI runtime — 0281-r4
+
+## Decision
+
+The workflow pins GitHub Copilot CLI to `1.0.70` and caches the complete
+workflow-local npm prefix:
+
+```text
+${{ github.workspace }}/.cache/copilot-cli
+```
+
+The exact key is scoped by cache schema, runner OS, runner architecture and
+Copilot CLI version. No broad restore key is used.
+
+## Execution
+
+```text
+cache restore
+-> exact hit: skip npm installation
+-> miss: npm install @github/copilot@1.0.70 into local prefix
+-> verify package version and executable
+-> call it through AUTODOC_COPILOT_COMMAND
+```
+
+`COPILOT_AUTO_UPDATE=false` preserves the pinned runtime.
+
+## Repository impact
+
+```text
+newicody/autodoc: modification required
+newicody/projects: modification required
+projects_repository_change_required: true
+```
+
+`newicody/projects` must deploy the workflow and allow `actions/cache@v4` in
+its selected-actions policy.
+
+## Boundaries
+
+```text
+Scheduler modification: none
+SQL/Qdrant modification: none
+GitHub mutation: none
+Copilot remains consultative and non-authoritative
+```
diff --git a/doc/manifests/MANIFEST_0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME.md b/doc/manifests/MANIFEST_0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME.md
new file mode 100644
index 0000000..c4b4cdf
--- /dev/null
+++ b/doc/manifests/MANIFEST_0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME.md
@@ -0,0 +1,19 @@
+# Manifest 0281-r4 — pinned cached Copilot CLI runtime
+
+Changed files:
+
+- `templates/github/autodoc-dual-artifact.yml`
+- `templates/github/projects-repository/.github/workflows/autodoc-controlled-research.yml`
+- `tests/rules/test_pinned_cached_copilot_cli_runtime_0281_rule.py`
+- `doc/architecture/GITHUB_PINNED_CACHED_COPILOT_CLI_RUNTIME_0281.md`
+- `doc/manifests/MANIFEST_0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME.md`
+- `PHASE0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME_REPORT.md`
+
+```text
+autodoc: change required
+projects: change required
+projects_repository_change_required: true
+```
+
+The bundle contains a separate deployment patch and policy JSON for
+`newicody/projects`.
diff --git a/templates/github/autodoc-dual-artifact.yml b/templates/github/autodoc-dual-artifact.yml
index 6dfe2f1..4bf2239 100644
--- a/templates/github/autodoc-dual-artifact.yml
+++ b/templates/github/autodoc-dual-artifact.yml
@@ -20,6 +20,9 @@ permissions:
 jobs:
   build-dual-artifacts:
     runs-on: ubuntu-latest
+    env:
+      AUTODOC_COPILOT_CLI_VERSION: "1.0.70"
+      AUTODOC_COPILOT_CLI_PREFIX: ${{ github.workspace }}/.cache/copilot-cli
 
     steps:
       - name: Checkout repository
@@ -40,14 +43,46 @@ jobs:
           python
           templates/github/scripts/build_autodoc_authoritative_request.py
 
-      - name: Install Copilot CLI
+      - name: Restore pinned Copilot CLI
+        id: autodoc-copilot-cli-cache
         if: ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true' }}
-        run: npm install --global @github/copilot
+        uses: actions/cache@v4
+        with:
+          path: ${{ env.AUTODOC_COPILOT_CLI_PREFIX }}
+          key: >-
+            autodoc-copilot-cli-v1-${{ runner.os }}-${{ runner.arch }}-${{
+            env.AUTODOC_COPILOT_CLI_VERSION }}
+
+      - name: Install pinned Copilot CLI on cache miss
+        if: >-
+          ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true'
+          && steps.autodoc-copilot-cli-cache.outputs.cache-hit != 'true' }}
+        shell: bash
+        run: |
+          set -euo pipefail
+          npm install             --prefix "$AUTODOC_COPILOT_CLI_PREFIX"             --no-audit             --no-fund             --omit=dev             "@github/copilot@$AUTODOC_COPILOT_CLI_VERSION"
+
+      - name: Verify pinned Copilot CLI
+        if: ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true' }}
+        env:
+          COPILOT_AUTO_UPDATE: "false"
+        shell: bash
+        run: |
+          set -euo pipefail
+          actual_version="$(
+            node -p               "require(process.env.AUTODOC_COPILOT_CLI_PREFIX +               '/node_modules/@github/copilot/package.json').version"
+          )"
+          test "$actual_version" = "$AUTODOC_COPILOT_CLI_VERSION"
+          test -x             "$AUTODOC_COPILOT_CLI_PREFIX/node_modules/.bin/copilot"
+          "$AUTODOC_COPILOT_CLI_PREFIX/node_modules/.bin/copilot"             --version
 
       - name: Build optional Copilot advisory
         if: ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true' }}
         env:
           GITHUB_TOKEN: ${{ github.token }}
+          COPILOT_AUTO_UPDATE: "false"
+          AUTODOC_COPILOT_COMMAND: >-
+            ${{ env.AUTODOC_COPILOT_CLI_PREFIX }}/node_modules/.bin/copilot
           AUTODOC_COPILOT_REQUIRED: "false"
           AUTODOC_REQUEST: out/authoritative_request.json
           AUTODOC_ADVISORY: out/copilot_advisory.json
diff --git a/templates/github/projects-repository/.github/workflows/autodoc-controlled-research.yml b/templates/github/projects-repository/.github/workflows/autodoc-controlled-research.yml
index f4d89d3..b84c8f8 100644
--- a/templates/github/projects-repository/.github/workflows/autodoc-controlled-research.yml
+++ b/templates/github/projects-repository/.github/workflows/autodoc-controlled-research.yml
@@ -37,6 +37,9 @@ permissions:
 jobs:
   build-dual-artifacts:
     runs-on: ubuntu-latest
+    env:
+      AUTODOC_COPILOT_CLI_VERSION: "1.0.70"
+      AUTODOC_COPILOT_CLI_PREFIX: ${{ github.workspace }}/.cache/copilot-cli
 
     steps:
       - name: Checkout Autodoc runtime
@@ -81,14 +84,46 @@ jobs:
           python
           autodoc-runtime/templates/github/scripts/build_autodoc_authoritative_request.py
 
-      - name: Install Copilot CLI
+      - name: Restore pinned Copilot CLI
+        id: autodoc-copilot-cli-cache
         if: ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true' }}
-        run: npm install --global @github/copilot
+        uses: actions/cache@v4
+        with:
+          path: ${{ env.AUTODOC_COPILOT_CLI_PREFIX }}
+          key: >-
+            autodoc-copilot-cli-v1-${{ runner.os }}-${{ runner.arch }}-${{
+            env.AUTODOC_COPILOT_CLI_VERSION }}
+
+      - name: Install pinned Copilot CLI on cache miss
+        if: >-
+          ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true'
+          && steps.autodoc-copilot-cli-cache.outputs.cache-hit != 'true' }}
+        shell: bash
+        run: |
+          set -euo pipefail
+          npm install             --prefix "$AUTODOC_COPILOT_CLI_PREFIX"             --no-audit             --no-fund             --omit=dev             "@github/copilot@$AUTODOC_COPILOT_CLI_VERSION"
+
+      - name: Verify pinned Copilot CLI
+        if: ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true' }}
+        env:
+          COPILOT_AUTO_UPDATE: "false"
+        shell: bash
+        run: |
+          set -euo pipefail
+          actual_version="$(
+            node -p               "require(process.env.AUTODOC_COPILOT_CLI_PREFIX +               '/node_modules/@github/copilot/package.json').version"
+          )"
+          test "$actual_version" = "$AUTODOC_COPILOT_CLI_VERSION"
+          test -x             "$AUTODOC_COPILOT_CLI_PREFIX/node_modules/.bin/copilot"
+          "$AUTODOC_COPILOT_CLI_PREFIX/node_modules/.bin/copilot"             --version
 
       - name: Build optional Copilot advisory
         if: ${{ vars.AUTODOC_COPILOT_ADVISORY_ENABLED == 'true' }}
         env:
           GITHUB_TOKEN: ${{ github.token }}
+          COPILOT_AUTO_UPDATE: "false"
+          AUTODOC_COPILOT_COMMAND: >-
+            ${{ env.AUTODOC_COPILOT_CLI_PREFIX }}/node_modules/.bin/copilot
           AUTODOC_COPILOT_REQUIRED: "false"
           AUTODOC_REQUEST: out/authoritative_request.json
           AUTODOC_ADVISORY: out/copilot_advisory.json
diff --git a/tests/rules/test_pinned_cached_copilot_cli_runtime_0281_rule.py b/tests/rules/test_pinned_cached_copilot_cli_runtime_0281_rule.py
new file mode 100644
index 0000000..e9d1d36
--- /dev/null
+++ b/tests/rules/test_pinned_cached_copilot_cli_runtime_0281_rule.py
@@ -0,0 +1,58 @@
+from pathlib import Path
+
+
+ROOT = Path(__file__).resolve().parents[2]
+WORKFLOWS = (
+    ROOT / "templates/github/autodoc-dual-artifact.yml",
+    ROOT
+    / "templates/github/projects-repository/.github/workflows"
+    / "autodoc-controlled-research.yml",
+)
+
+
+def test_copilot_cli_is_pinned_cached_and_not_globally_reinstalled() -> None:
+    for path in WORKFLOWS:
+        text = path.read_text(encoding="utf-8")
+        for required in (
+            'AUTODOC_COPILOT_CLI_VERSION: "1.0.70"',
+            "AUTODOC_COPILOT_CLI_PREFIX:",
+            "uses: actions/cache@v4",
+            "id: autodoc-copilot-cli-cache",
+            "steps.autodoc-copilot-cli-cache.outputs.cache-hit != 'true'",
+            '"@github/copilot@$AUTODOC_COPILOT_CLI_VERSION"',
+            "--prefix",
+            "--no-audit",
+            "--no-fund",
+            "--omit=dev",
+            'COPILOT_AUTO_UPDATE: "false"',
+            "AUTODOC_COPILOT_COMMAND:",
+            "/node_modules/.bin/copilot",
+            "actual_version",
+        ):
+            assert required in text
+        for forbidden in (
+            "npm install --global @github/copilot",
+            "npm install -g @github/copilot",
+            "@github/copilot@latest",
+            "@github/copilot@prerelease",
+            "restore-keys:",
+        ):
+            assert forbidden not in text
+
+
+def test_cache_key_is_exactly_scoped_by_platform_and_version() -> None:
+    for path in WORKFLOWS:
+        text = path.read_text(encoding="utf-8")
+        assert "autodoc-copilot-cli-v1-${{ runner.os }}" in text
+        assert "${{ runner.arch }}" in text
+        assert "env.AUTODOC_COPILOT_CLI_VERSION" in text
+
+
+def test_projects_change_is_explicitly_declared() -> None:
+    report = (
+        ROOT
+        / "PHASE0281_R4_PINNED_CACHED_COPILOT_CLI_RUNTIME_REPORT.md"
+    ).read_text(encoding="utf-8")
+    assert "projects_repository_change_required: true" in report
+    assert "actions/cache@v4" in report
+    assert "newicody/projects" in report
