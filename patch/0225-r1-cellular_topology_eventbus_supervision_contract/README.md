# 0225-r1-cellular_topology_eventbus_supervision_contract

Documentation/rule patch locking cellular topology, locality grouping, and observed movement semantics for passive EventBus supervision.

Apply with:

```bash
python apply_patch_queue.py --patch 0225-r1-cellular_topology_eventbus_supervision_contract --dry-run --allow-dirty
```
