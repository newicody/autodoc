# 0274-r1 — Existing Scheduler laboratory visit binding

Activates the deterministic fake laboratory through the one existing Scheduler path.

```bash
python apply_patch_queue.py \
  --patch 0274-r1-existing_scheduler_laboratory_visit_binding \
  --dry-run \
  --allow-dirty
```
