# 0163 — GitHub external artifact existing builders

Apply:

```text
tar -xJf autodoc_patch_0163-github_external_artifact_existing_builders.tar.xz
python apply_patch_queue.py --patch 0163-github_external_artifact_existing_builders --dry-run --allow-dirty
python apply_patch_queue.py --patch 0163-github_external_artifact_existing_builders --commit --push --allow-dirty
```
