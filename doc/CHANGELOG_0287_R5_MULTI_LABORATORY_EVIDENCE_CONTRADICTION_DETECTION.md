# Changelog 0287-r5

- add explicit contradiction policy contract;
- detect support/opposition contradictions;
- detect policy-declared exclusive values;
- detect same-content claim interpretation conflicts;
- preserve stable evidence, canonical, source and laboratory references;
- keep every contradiction unresolved for r6;
- simplify the cumulative `newicody/projects` installation guide;
- document automatic Actions `GITHUB_TOKEN` and local `gh` token reuse;
- clarify that `AUTODOC_COPILOT_TOKEN` is not required;
- make r1-r4 audit progression rules cumulative through r5.
