digraph specialist_capability_growth_closed_loop_smoke_0285 {
    rankdir=LR;
    graph [label="0285-r8 specialist capability growth closed-loop smoke", labelloc=t];
    node [shape=box];

    evidence [label="r2 evidence refs\nproposal"];
    revision [label="r3 immutable\nrevision lineage"];
    gate [label="r4 operator gate\napprove / reject / defer"];
    blocked [label="reject + defer\nblocked before history", shape=octagon];
    sql [label="r5 durable history\nSQL authority"];
    scheduler [label="r6 selection handler\nexisting Scheduler authority"];
    laboratory [label="0284-r5 existing chain\nlocal fake laboratory"];
    eventbus [label="r7 EventBus projection\nobservation only"];
    passive [label="r7 passive read model\nno control"];
    result [label="r8 correlated proof\nphase_0285_closed"];

    evidence -> revision -> gate;
    gate -> blocked [label="reject / defer"];
    gate -> sql [label="approve"];
    sql -> scheduler -> laboratory -> eventbus -> passive -> result;

    qdrant [label="Qdrant\nprojection/recall only", style=dashed];
    github [label="GitHub Projects\nno mutation", style=dashed];
    laboratory -> qdrant [style=dashed, label="existing 0284 closure"];
    result -> github [style=dashed, label="not published"];
}
