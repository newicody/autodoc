digraph github_actions_ready_run_copilot_issue_publication_0287 {
  rankdir=LR;
  node [shape=box];

  project [label="ProjectV2 inference-qualified Issue"];
  dispatch [label="Local controlled dispatcher"];
  workflow [label="Projects workflow\nissues: read"];
  artifacts [label="3 correlated artifacts"];
  fetch [label="Local read-only fetch"];
  ready [label="ready_run"];
  raw [label="Durable raw dataset"];
  preview [label="Existing v2 preview builder"];
  plan [label="Existing Issue publication planner"];
  gates [label="Local policy + operator + env gates"];
  comment [label="GitHub Issue advisory comment"];
  readback [label="Verified readback"];
  receipt [label="Local completion receipt"];

  project -> dispatch -> workflow -> artifacts -> fetch -> ready -> raw;
  raw -> preview -> plan -> gates -> comment -> readback -> receipt;

  workflow -> comment [style=dashed, color=red, label="forbidden direct mutation"];
}
