digraph context_revision_scheduler_execution_0287_r7_r8_r6 {
  rankdir=LR;
  graph [label="0287-r7-r8-r6 — authorized context-impact execution", labelloc=t];
  node [shape=box];

  sql [label="SQL context revision\nauthority"];
  plan [label="r8-r5 impact plan\neffect-free"];
  auth [label="Scheduler authorization\nexact plan SHA-256"];
  event [label="existing Dispatcher/Event\nCONTEXT_IMPACT_EXECUTION"];
  handler [label="SchedulerContextImpactExecutionHandler"];
  mutation [label="Scheduler-owned mutation port\nidempotent task transitions"];
  result [label="EventBus observation\nexecution result"];
  labs [label="Scheduler-issued laboratory\ncontext update"];
  route [label="existing route adapter\nexplicit transport transition only"];
  proxy [label="ControlProxy\ntransport only"];

  sql -> plan;
  plan -> auth;
  auth -> event;
  event -> handler;
  handler -> mutation;
  handler -> result;
  handler -> labs;
  handler -> route [label="only if requested"];
  route -> proxy;
}
