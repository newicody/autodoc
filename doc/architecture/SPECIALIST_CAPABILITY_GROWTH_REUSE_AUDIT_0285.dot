digraph specialist_capability_growth_reuse_audit_0285 {
    rankdir=LR;
    graph [label="0285-r1 specialist capability growth reuse audit", labelloc=t];
    node [shape=box];

    evidence [label="existing results / evidence\nreference-only inputs"];
    proposal [label="0285-r2\nimmutable proposal"];
    lineage [label="0285-r3\nportable revision lineage"];
    gate [label="0285-r4\noperator decision gate"];
    sql [label="0285-r5\nSQL append-only history\nauthority"];
    scheduler [label="0285-r6\nexisting Scheduler\napproved revision only"];
    laboratory [label="existing laboratory\ncompatibility + execution"];
    observation [label="0285-r7\nEventBus / PassiveSupervisor / Cell Lens\nobservation only"];
    smoke [label="0285-r8\nclosed-loop smoke"];
    qdrant [label="Qdrant\nprojection / recall only", shape=ellipse];
    github [label="GitHub Projects / Copilot\nreview + advisory only", shape=ellipse];

    evidence -> proposal;
    proposal -> lineage;
    lineage -> gate;
    gate -> sql [label="approve/reject/defer"];
    sql -> scheduler [label="approved revision refs"];
    scheduler -> laboratory;
    laboratory -> observation;
    observation -> smoke;
    sql -> qdrant [style=dashed, label="reference projection"];
    github -> gate [style=dashed, label="operator workflow"];

    forbidden_registry [label="no global specialist registry", shape=note];
    forbidden_manager [label="no LaboratoryManager", shape=note];
    forbidden_learning [label="no automatic self-authorization", shape=note];
}
