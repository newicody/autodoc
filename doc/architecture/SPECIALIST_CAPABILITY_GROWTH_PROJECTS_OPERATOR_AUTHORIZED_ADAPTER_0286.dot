digraph phase_0286_r6 {
  rankdir=LR;
  plan [label="r5 publication plan"];
  preview [label="preview (default)"];
  gate [label="approve + execute + exact digest"];
  port [label="existing GitHub execution port"];
  issue [label="Issue comment"];
  fields [label="ProjectV2 fields"];
  evidence [label="bounded result"];
  plan -> preview -> gate;
  gate -> port [label="authorized"];
  port -> issue;
  port -> fields;
  issue -> evidence;
  fields -> evidence;
}
