digraph specialist_capability_growth_observation_projection_0285 {
    rankdir=LR;
    node [shape=box];

    proposal [label="r2 proposal + evidence"];
    revision [label="r3 portable revision"];
    decision [label="r4 operator decision"];
    history [label="r5 SQL durable history"];
    selection [label="r6 Scheduler selection"];
    event [label="SPECIALIST_REVISION_SELECTION_RESULT\nimmutable observation event"];
    bus [label="existing EventBus\nobservation only"];
    supervisor [label="r7 PassiveSupervisor read model\nobservation only"];
    celllens [label="existing Cell Lens bridge"];
    execution [label="laboratory execution\nNOT performed", style=dashed];

    proposal -> revision -> decision -> history -> selection -> event -> bus;
    bus -> supervisor;
    bus -> celllens;
    supervisor -> execution [style=dashed];
}
