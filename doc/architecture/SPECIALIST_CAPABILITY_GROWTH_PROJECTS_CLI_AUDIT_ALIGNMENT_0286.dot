digraph phase_0286_r6_r1 {
  rankdir=LR;
  plan [label="r5 plan JSON"];
  verify [label="schema + digest verification"];
  preview [label="preview (default)"];
  gate [label="approve + execute + exact digest"];
  gh [label="existing gh api boundary"];
  issue [label="Issue comment"];
  fields [label="ProjectV2 fields"];
  plan -> verify -> preview -> gate;
  gate -> gh [label="authorized"];
  gh -> issue;
  gh -> fields;
}
