digraph specialist_capability_growth_proposal_0285 {
  rankdir=LR;
  graph [label="0285-r2 specialist capability growth proposal contract", labelloc=t];
  node [shape=box];

  execution [label="existing execution / report / observation"];
  evidence [label="SpecialistCapabilityEvidenceRef\ntyped ref + SHA-256"];
  proposal [label="SpecialistCapabilityGrowthProposal\nnon-authoritative"];
  operator_gate [label="future operator decision\n0285-r4", style=dashed];
  revision [label="future immutable revision lineage\n0285-r3", style=dashed];
  scheduler [label="Scheduler\nonly orchestration authority", shape=note];
  sql [label="SQL\ndurable authority", shape=cylinder];
  qdrant [label="Qdrant\nprojection / recall only", shape=cylinder];

  execution -> evidence;
  evidence -> proposal;
  proposal -> revision [style=dashed, label="candidate lineage"];
  proposal -> operator_gate [style=dashed, label="requires explicit decision"];
  operator_gate -> scheduler [style=dashed, label="future approved selection"];
  operator_gate -> sql [style=dashed, label="future append-only history"];
  sql -> qdrant [style=dotted, label="references only"];
}
