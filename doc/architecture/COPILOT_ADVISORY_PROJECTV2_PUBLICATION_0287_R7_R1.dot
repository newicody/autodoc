digraph phase_0287_r7_r1 {
  rankdir=LR;
  workflow [label="Actions producer"];
  request [label="authoritative request"];
  advisory [label="Copilot advisory\nnon-authoritative"];
  manifest [label="dual manifest"];
  validate [label="correlation + SHA-256"];
  preview [label="publication preview"];
  operator [label="operator approve"];
  project [label="ProjectV2 field plan"];
  execute [label="confirmed mutation"];
  workflow -> request;
  workflow -> advisory;
  workflow -> manifest;
  request -> validate;
  advisory -> validate;
  manifest -> validate;
  validate -> preview -> operator -> project -> execute;
}
