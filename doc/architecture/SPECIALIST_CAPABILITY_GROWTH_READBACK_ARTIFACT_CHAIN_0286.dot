digraph phase_0286_r7_r1 {
  rankdir=LR;
  fixture [label="local fixture builder"];
  plan [label="r5 plan JSON"];
  execution [label="r6 result JSON"];
  snapshots [label="comment + fields snapshots"];
  verifier [label="r7 verifier"];
  local [label="snapshot_ready"];
  live [label="deployment_ready\nlive query-only only"];
  fixture -> plan;
  fixture -> execution;
  fixture -> snapshots;
  plan -> verifier;
  execution -> verifier;
  snapshots -> verifier;
  verifier -> local;
  verifier -> live [style=dashed];
}
