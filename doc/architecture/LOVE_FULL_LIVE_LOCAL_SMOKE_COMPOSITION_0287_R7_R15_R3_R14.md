# Full live local smoke composition

```text
dual artifacts -> operator gate -> existing Scheduler -> two specialists
    -> r12 SQL authority + E5/Qdrant projection
    -> r13 query embedding + dense/sparse recall + SQL rehydration
    -> liaison synthesis -> publication preview + simulated readback
```

No remote mutation is enabled in this unit.
