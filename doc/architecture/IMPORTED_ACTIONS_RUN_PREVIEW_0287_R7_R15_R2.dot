digraph ImportedActionsRunPreview0287R7R15R2 {
  rankdir=LR;
  node [shape=box];

  run [label="Completed real\nActions run"];
  list [label="Read artifact metadata"];
  artifacts [label="3 exact artifacts\nrequest + advisory + manifest"];
  download [label="gh run download\nread-only"];
  preflight [label="Existing dual-artifact preflight\nrequest + advisory + manifest"];
  gate [label="Explicit operator decision\npromote or merge"];
  factory [label="Explicit existing-runtime factory\nmodule:function"];
  ports [label="Scheduler + SQL + real E5-384\n+ real Qdrant ports"];
  lifecycle [label="Scheduler lifecycle\ntool-bounded or externally-managed"];
  attestation [label="Immutable backend attestation\nwith evidence refs"];
  r14 [label="Existing r14 composition"];
  receipts [label="2 real projection receipts\nchecked against attestation"];
  plan [label="r13 publication plan\nplan_digest"];
  preview [label="r15-r1 mandatory preview\nIssue + ProjectV2 reads"];
  output [label="love-r14-result-RUN_ID.json\ncompatible with publisher"];
  execute [label="Separate controlled execute\nr15-r3", style=dashed];

  run -> list -> artifacts -> download -> preflight -> gate -> r14;
  factory -> ports -> r14;
  factory -> lifecycle -> r14;
  factory -> attestation -> receipts;
  r14 -> receipts -> plan -> preview -> output;
  output -> execute [label="operator confirms digest"];
}
