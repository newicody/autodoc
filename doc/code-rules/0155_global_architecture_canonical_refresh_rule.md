# Code rule — 0155 global architecture canonical refresh

The canonical architecture graph is `doc/docs/architecture/00_global.dot`.

Future architecture updates must preserve the existing hierarchy and update the canonical graph or one of its linked subgraphs instead of adding isolated summary graphs.

Every significant phase from 0155 onward must include a changelog.

Historical graphs must be preserved unless there is a specific migration note explaining why a graph is replaced.
