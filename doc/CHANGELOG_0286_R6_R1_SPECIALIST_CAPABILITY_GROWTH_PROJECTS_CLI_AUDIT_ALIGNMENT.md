# Changelog 0286-r6-r1

- add the concrete r6 operator CLI required by the reuse audit;
- recompute and verify the r5 plan digest before preview or execution;
- keep preview as the default;
- reuse `gh api` for Issue and ProjectV2 operations;
- preserve SQL and Scheduler authority boundaries.
