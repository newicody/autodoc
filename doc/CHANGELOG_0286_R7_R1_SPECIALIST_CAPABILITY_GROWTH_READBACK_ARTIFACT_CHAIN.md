# Changelog 0286-r7-r1

- add `--output` to the r6 operator CLI;
- persist successful and failed r6 reports;
- add actionable r7 input preflight;
- add a local-only readback fixture builder;
- separate fixture validation from live deployment readiness;
- keep the `newicody/projects` installation bundle unchanged.
