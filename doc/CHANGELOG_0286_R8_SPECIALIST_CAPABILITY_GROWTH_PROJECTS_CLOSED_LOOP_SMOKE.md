# Changelog 0286-r8

- add immutable r5/r6/r7 closed-loop smoke result;
- verify plan, execution, comment, fields and readback digests;
- distinguish deterministic local closure from live deployment closure;
- add a JSON-artifact CLI with optional live-readback requirement;
- make the historical r7 audit progression rule cumulative;
- close the 0286 audit without adding a new authority or backend.
