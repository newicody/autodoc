# Changelog — 0284-r1-r3-r4

- restored `github_action_on_ticket_event` for the read-only artifact-ingestion examples;
- kept `workflow_dispatch` exclusively in the dedicated outbound Projects configuration;
- restored the optional 0272 Actions-readiness template contract;
- migrated the compatibility assertions to the repaired trigger boundary;
- updated the cumulative `newicody/projects` installation guide;
- changed no runtime tool, Scheduler, SQL, OpenVINO, Qdrant or EventBus component.
