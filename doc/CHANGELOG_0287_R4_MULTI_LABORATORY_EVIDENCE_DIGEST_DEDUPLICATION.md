# Changelog 0287-r4

- add immutable content-digest groups;
- add deterministic canonical evidence selection;
- preserve duplicate evidence aliases and provenance references;
- derive deterministic source-identity digests from `source_ref`;
- retain cross-source and cross-laboratory corroboration;
- retain claim variants for r5 contradiction detection;
- block deduplication when r3 provenance validation fails;
- correct the r3 typed-reference regex for payloads beginning with `s`;
- keep historical r1/r2/r3 audit progression rules cumulative.
