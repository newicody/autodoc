# Changelog 0284-r7

- compose the existing 0281 dual-artifact assembly with 0284-r6;
- require the validated Copilot context and authoritative candidate inside the
  portable specialist command;
- reuse the 0281 controlled Issue publication planner;
- produce a no-write ProjectV2 field preview matching the bundle copied into
  `newicody/projects`;
- preserve the unique Scheduler, SQL authority, Qdrant reference-only recall
  and observation-only EventBus boundaries;
- perform no GitHub or ProjectV2 mutation.
