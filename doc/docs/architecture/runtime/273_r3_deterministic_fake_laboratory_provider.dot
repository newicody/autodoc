digraph DeterministicFakeLaboratoryProvider0273R3 {
  rankdir=LR;
  node [shape=box];

  Request [label="LaboratoryVisitRequest"];
  Scheduler [label="Scheduler / Policy / Queue"];
  ExistingRegistry [label="existing registry 0257"];
  Registration [label="SchedulerOwnedRuntimeComponentRegistration"];
  Handler [label="Dispatcher / Handler"];
  FakeProvider [label="deterministic local fake provider"];
  VisitResult [label="immutable LaboratoryVisitResult"];
  EventBus [label="future observation facts only"];
  PassiveSupervisor [label="PassiveSupervisor"];
  VisPy [label="Cell Lens / VisPy"];

  Request -> Scheduler;
  ExistingRegistry -> Registration;
  Registration -> Handler;
  Scheduler -> Handler;
  Handler -> FakeProvider;
  FakeProvider -> VisitResult;
  VisitResult -> EventBus [style=dashed, label="0274"];
  EventBus -> PassiveSupervisor;
  PassiveSupervisor -> VisPy;

  Closed [shape=note, label="No network / SQL / Qdrant / GitHub / real model"];
  FakeProvider -> Closed [style=dotted, arrowhead=none];
}
