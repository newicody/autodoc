# Phase 7.12 — External Probe Bundle CLI Dry-Run

Phase 7.12 adds a command-line entrypoint for the local external probe bundle.

The CLI is dry-run by default and writes the bundle only when `--apply` is
provided.
