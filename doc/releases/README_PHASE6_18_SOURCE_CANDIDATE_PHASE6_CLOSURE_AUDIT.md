# Phase 6.18 — SourceCandidate Phase 6 Closure Audit

Phase 6.18 adds the final local audit before Part 7.

The audit reads:

```text
handoff_manifest.json
projection_preview.json
projection_gate_report.json
```

and writes:

```text
phase6_closure_audit.json
```

A passing audit means the local SourceCandidate operator/projection chain is ready for the next design boundary.
