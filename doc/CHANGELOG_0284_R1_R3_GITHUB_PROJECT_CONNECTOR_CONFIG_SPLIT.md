# Changelog — 0284-r1-r3

- separated ProjectV2 query-only and Projects workflow-dispatch examples;
- documented explicit `--config` selection for the dedicated outbound config;
- retained safe `false` defaults for both remote-dispatch gates;
- aligned stale example repository references with `newicody/projects`;
- added the cumulative `projects-repository/INSTALLATION.md`;
- documented the Autodoc / Projects ownership boundary;
- added executable rule coverage for the split and installation guide.
