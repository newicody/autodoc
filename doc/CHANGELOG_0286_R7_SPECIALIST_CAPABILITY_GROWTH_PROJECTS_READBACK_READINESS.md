# Changelog 0286-r7

- add immutable Projects readback evidence;
- correlate the r5 plan and r6 execution result;
- verify the append-only Issue comment marker, body, digest and identifier;
- verify the nine ProjectV2 field values;
- add local-snapshot and live query-only modes;
- reserve deployment readiness for successful live readback;
- preserve SQL, Scheduler, Qdrant and GitHub authority boundaries.
